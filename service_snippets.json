[
  {
    "file": "sales-quotations/sales-quotations.service.ts",
    "matches": [
      {
        "type": "createMany",
        "model": "salesQuotationItem",
        "snippet": "otalDiscount,\n          status: SalesQuotationStatus.OPEN,\n          currency: dto.currency ?? 'BOB',\n          exchangeRate: dto.exchangeRate ?? 1,\n          createdById,\n        },\n      });\n\n      await tx.salesQuotationItem.createMany({\n        data: lineResults.map((l, index) => ({\n          tenantId,\n          quotationId: quotation.id,\n          itemId: l.itemId,\n          itemCode: l.itemCode,\n          itemName: l.itemName,\n          lineNum: index,\n          quantity: l.quantity,\n          price: l.price,\n          discountPct: l.discountPct,\n          discountAmt: l.discountAmt,\n          discountTotal: l.discountTotal,\n          priceNet: l.priceNet,\n          subtotal: l.subtotal,\n          openQty: l.quantity,\n          orderedQty: 0,\n          cost: l.cost,\n          totalCost: l.totalCost,\n          weight: l.weight,\n          totalWeight: l.totalWeight,\n          warehouseId: l.warehouseId ?? null,\n          taxIndicatorId: l.taxIndicatorId ?? null,\n          taxRate: l.taxRate,\n          taxAmount: l.taxAm"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "      }\n          // openQty = quantity - orderedQty (pendiente no comprometido en pedidos)\n          const newOpenQty =\n            Number(incoming.quantity) - Number(existing.orderedQty);\n          await tx.salesQuotationItem.update({\n            where: { id: incoming.id },\n            data: {\n              itemId: incoming.itemId,\n              itemCode: catalogItem.code,\n              itemName: catalogItem.name,\n              quantity: incoming.quantity,\n              openQty: newOpenQty, // recalculado al cambiar quantity\n              price,\n              discountPct:\n                discountMode === 'line' ? (incoming.discountPct ?? null) : null,\n              discountAmt:\n                discountMode === 'line' ? (incoming.discountAmt ?? null) : null,\n              discountTotal: lineCalc.discountTotal,\n              priceNet: lineCalc.priceNet,\n              subtotal: lineCalc.lineFinal,\n              cost,\n              totalCost,\n              weight,\n              totalWeight,\n              warehouseId: line"
      },
      {
        "type": "create",
        "model": "salesQuotationItem",
        "snippet": "ndicatorId,\n              taxRate,\n              taxAmount: lineCalc.taxAmount,\n              customFields: incoming.customFields ?? undefined,\n            },\n          });\n        } else {\n          await tx.salesQuotationItem.create({\n            data: {\n              tenantId: tenantId,\n              quotationId: id,\n              itemId: incoming.itemId,\n              itemCode: catalogItem.code,\n              itemName: catalogItem.name,\n              lineNum:\n                currentLines.length +\n                lineResults.findIndex((lr) => lr.incoming === incoming),\n              quantity: incoming.quantity,\n              price,\n              discountPct:\n                discountMode === 'line' ? (incoming.discountPct ?? null) : null,\n              discountAmt:\n                discountMode === 'line' ? (incoming.discountAmt ?? null) : null,\n              discountTotal: lineCalc.discountTotal,\n              priceNet: lineCalc.priceNet,\n              subtotal: lineCalc.lineFinal,\n              openQty: incoming.quan"
      }
    ]
  },
  {
    "file": "sales-orders/sales-orders.service.ts",
    "matches": [
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "dBaseFields(\n            DocumentType.SALES_QUOTATION,\n            quotation.id,\n            qItem.id,\n            qItem.lineNum ?? index,\n            line.quantity,\n          ),\n        });\n\n        await tx.salesQuotationItem.update({\n          where: { id: qItem.id },\n          data: {\n            orderedQty: { increment: line.quantity },\n            openQty: { decrement: line.quantity },\n          },\n        });\n      }\n\n      let docTotals: {\n        total: number;\n        subtotal: number;\n        tax: number;\n        totalDiscount: number;\n      };\n      if (discountMode === 'header') {\n        const dt = calcDocumentTotalsWithIndicators(\n          lineCalcs.map((lc) => ({\n            lineNet: lc.lineNet,\n            taxRate: lc.taxRate ?? 0,\n            taxAmount: lc.taxAmount ?? 0,\n            lineFinal: lc.lineFinal,\n            isInclusive: lc.isInclusive ?? true,\n          })),\n          invoiced,\n          draft.headerDiscountPct,\n          draft.headerDiscountAmt,\n        );\n        docTotals = {\n         "
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "\n        DocumentType.SALES_QUOTATION,\n        quotation.id,\n        DocumentType.SALES_ORDER,\n        order.id,\n      );\n\n      for (const oi of order.items) {\n        if (oi.baseLineId) {\n          await tx.salesQuotationItem.update({\n            where: { id: oi.baseLineId },\n            data: {\n              targetDocType: DocumentType.SALES_ORDER,\n              targetDocId: order.id,\n              targetLineId: oi.id,\n            },\n          });\n\n          const qItem = quotation.items.find((i) => i.id === oi.baseLineId)!;\n          // orderedQty ya fue incrementado en el loop anterior → leer valor actualizado\n          const updatedQItem = await tx.salesQuotationItem.findUnique({\n            where: { id: qItem.id },\n          });\n          if (updatedQItem) {\n            await refreshLineStatus(\n              tx,\n              'salesQuotationItem',\n              qItem.id,\n              Number(updatedQItem.openQty),\n            );\n          }\n        }\n      }\n\n      // ── Stock: actualizar committed por almacén ──"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "       DocumentType.SALES_QUOTATION,\n              line.quotationId,\n              qItem.id,\n              qItem.lineNum ?? index,\n              line.quantity,\n            ),\n          });\n\n          await tx.salesQuotationItem.update({\n            where: { id: qItem.id },\n            data: {\n              orderedQty: { increment: line.quantity },\n              openQty: { decrement: line.quantity },\n            },\n          });\n          continue;\n        }\n\n        const lineCalc = calcLineWithIndicator({\n          price,\n          quantity: line.quantity,\n          invoiced,\n          discountPct,\n          discountAmt,\n          taxRate: lineTaxRate,\n          isInclusive: lineIsIncl,\n        });\n        lineCalcs.push({ ...lineCalc, taxIndicatorId: lineTaxIndId });\n\n        const cost = await getAvgCost(\n          tx,\n          tenantId,\n          qItem.itemId,\n          lineWh,\n          qItem.item.cost != null ? Number(qItem.item.cost) : null,\n        );\n        const weight =\n          qItem.item.weight != null ?"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "eFields(\n            DocumentType.SALES_QUOTATION,\n            line.quotationId,\n            qItem.id,\n            qItem.lineNum ?? index,\n            line.quantity,\n          ),\n        });\n\n        await tx.salesQuotationItem.update({\n          where: { id: qItem.id },\n          data: {\n            orderedQty: { increment: line.quantity },\n            openQty: { decrement: line.quantity },\n          },\n        });\n      }\n\n      const docTotals = calcDocumentTotalsFromLines(lineCalcs);\n\n      const baseTotals = calcBaseCurrencyTotals(\n        docTotals,\n        Number(payload.exchangeRate ?? 1),\n      );\n\n      const order = await tx.salesOrder.create({\n        data: {\n          tenantId,\n          customFields: payload.customFields ?? {},\n          code: await this.generateCode(tx, tenantId),\n          quotationId: null, // consolidado — sin cotización única origen\n          partnerId: payload.partnerId,\n          salesPersonId: payload.salesPersonId ?? null,\n          contactPerson: payload.contactPerson,\n          "
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "ntType.SALES_QUOTATION,\n          quotationId,\n          DocumentType.SALES_ORDER,\n          order.id,\n        );\n      }\n\n      for (const oi of order.items) {\n        if (oi.baseLineId) {\n          await tx.salesQuotationItem.update({\n            where: { id: oi.baseLineId },\n            data: {\n              targetDocType: DocumentType.SALES_ORDER,\n              targetDocId: order.id,\n              targetLineId: oi.id,\n            },\n          });\n          const updatedQItem = await tx.salesQuotationItem.findUnique({\n            where: { id: oi.baseLineId },\n          });\n          if (updatedQItem)\n            await refreshLineStatus(\n              tx,\n              'salesQuotationItem',\n              oi.baseLineId,\n              Number(updatedQItem.openQty),\n            );\n        }\n      }\n\n      const whItemMap = new Map<number, Set<number>>();\n      for (const oi of orderItems) {\n        const wId = oi.warehouseId ?? effectiveWarehouseId;\n        if (wId) {\n          if (!whItemMap.has(wId)) whItemMap.set(wId, "
      },
      {
        "type": "create",
        "model": "salesOrderItem",
        "snippet": "urrency ?? 'BOB',\n          exchangeRate: payload.exchangeRate ?? 1,\n          createdById: createdById ?? null,\n        },\n      });\n\n      for (const [index, lr] of lineResults.entries()) {\n        await tx.salesOrderItem.create({\n          data: {\n            tenantId,\n            orderId: order.id,\n            itemId: lr.itemId,\n            itemName: lr.itemName,\n            itemCode: lr.itemCode,\n            quantity: lr.quantity,\n            price: lr.price,\n            discountPct: lr.discountPct,\n            discountAmt: lr.discountAmt,\n            discountTotal: lr.discountTotal,\n            priceNet: lr.priceNet,\n            subtotal: lr.subtotal,\n            cost: lr.cost,\n            totalCost: lr.totalCost,\n            weight: lr.weight,\n            totalWeight: lr.totalWeight,\n            warehouseId: lr.warehouseId,\n            taxIndicatorId: lr.taxIndicatorId ?? null,\n            taxRate: lr.taxRate,\n            taxAmount: lr.taxAmount,\n            lineNum: index,\n            openQty: lr.quantity, /"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "}\n        wasAutoClosedFull = allFullyCovered;\n      }\n\n      for (const line of order.items) {\n        if (line.quotationItemId) {\n          // orderedQty↓ + openQty↑ al cancelar el pedido\n          await tx.salesQuotationItem.update({\n            where: { id: line.quotationItemId },\n            data: {\n              orderedQty: { decrement: Number(line.quantity) },\n              openQty: { increment: Number(line.quantity) },\n            },\n          });\n\n          await clearTargetOnSourceLine(\n            tx,\n            'salesQuotationItem',\n            line.quotationItemId,\n          );\n\n          const qItem = await tx.salesQuotationItem.findUnique({\n            where: { id: line.quotationItemId },\n          });\n          if (qItem) {\n            await refreshLineStatus(\n              tx,\n              'salesQuotationItem',\n              qItem.id,\n              Number(qItem.openQty),\n            );\n          }\n        }\n      }\n\n      await tx.salesOrder.update({\n        where: { id: orderId },\n        data: {\n   "
      },
      {
        "type": "create",
        "model": "salesOrderItem",
        "snippet": "eight != null\n          ? Number(quotationItem.item.weight)\n          : null;\n\n      const existingCount = await tx.salesOrderItem.count({\n        where: { orderId },\n      });\n\n      const newItem = await tx.salesOrderItem.create({\n        data: {\n          tenantId: tenantId as number,\n          orderId,\n          quotationItemId: quotationItem.id,\n          itemId: quotationItem.itemId,\n          itemName: quotationItem.item.name,\n          itemCode: quotationItem.item.code,\n          quantity: dto.quantity,\n          price,\n          discountPct: dto.discountPct ?? null,\n          discountAmt: dto.discountAmt ?? null,\n          discountTotal: lineCalc.discountTotal,\n          priceNet: lineCalc.priceNet,\n          subtotal: lineCalc.lineFinal,\n          cost,\n          totalCost: cost != null ? cost * dto.quantity : null,\n          weight,\n          totalWeight: weight != null ? weight * dto.quantity : null,\n          lineNum: existingCount,\n          projectCode: dto.projectCode ?? null,\n          dimension1: d"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "e.SALES_QUOTATION,\n            order.quotationId!,\n            quotationItem.id,\n            quotationItem.lineNum ?? existingCount,\n            dto.quantity,\n          ),\n        },\n      });\n\n      await tx.salesQuotationItem.update({\n        where: { id: quotationItem.id },\n        data: {\n          targetDocType: DocumentType.SALES_ORDER,\n          targetDocId: orderId,\n          targetLineId: newItem.id,\n        },\n      });\n\n      await tx.salesQuotationItem.update({\n        where: { id: quotationItem.id },\n        data: {\n          orderedQty: { increment: dto.quantity },\n          openQty: { decrement: dto.quantity },\n        },\n      });\n\n      const updatedQItem = await tx.salesQuotationItem.findUnique({\n        where: { id: quotationItem.id },\n      });\n      if (updatedQItem) {\n        await refreshLineStatus(\n          tx,\n          'salesQuotationItem',\n          quotationItem.id,\n          Number(updatedQItem.openQty),\n        );\n      }\n\n      await tx.salesOrder.update({\n        where: { id: orderId },\n"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "    where: { id: quotationItem.id },\n        data: {\n          targetDocType: DocumentType.SALES_ORDER,\n          targetDocId: orderId,\n          targetLineId: newItem.id,\n        },\n      });\n\n      await tx.salesQuotationItem.update({\n        where: { id: quotationItem.id },\n        data: {\n          orderedQty: { increment: dto.quantity },\n          openQty: { decrement: dto.quantity },\n        },\n      });\n\n      const updatedQItem = await tx.salesQuotationItem.findUnique({\n        where: { id: quotationItem.id },\n      });\n      if (updatedQItem) {\n        await refreshLineStatus(\n          tx,\n          'salesQuotationItem',\n          quotationItem.id,\n          Number(updatedQItem.openQty),\n        );\n      }\n\n      await tx.salesOrder.update({\n        where: { id: orderId },\n        data: { updatedById: updatedById ?? null },\n      });\n\n      await this.recalculateTotals(orderId, tenantId, tx);\n      await recalcCommitted(\n        tx,\n        tenantId,\n        quotationItem.itemId,\n        order.warehouseId,\n   "
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "\n        useSinTaxCalculation,\n      });\n\n      const updatedCost = await getAvgCost(\n        tx,\n        tenantId,\n        line.itemId,\n        line.warehouseId ?? null,\n        null,\n      );\n      await tx.salesOrderItem.update({\n        where: { id: lineId },\n        data: {\n          quantity: dto.quantity,\n          discountPct:\n            dto.discountPct != null ? dto.discountPct : line.discountPct,\n          discountAmt:\n            dto.discountAmt != null ? dto.discountAmt : line.discountAmt,\n          discountTotal: lineCalc.discountTotal,\n          priceNet: lineCalc.priceNet,\n          subtotal: lineCalc.lineFinal,\n          cost: updatedCost,\n          totalCost: updatedCost != null ? updatedCost * dto.quantity : null,\n          totalWeight:\n            line.weight != null ? Number(line.weight) * dto.quantity : null,\n          baseQty: dto.quantity,\n          projectCode: dto.projectCode ?? null,\n          dimension1: dto.dimension1 ?? null,\n          dimension2: dto.dimension2 ?? null,\n        },\n    "
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "         baseQty: dto.quantity,\n          projectCode: dto.projectCode ?? null,\n          dimension1: dto.dimension1 ?? null,\n          dimension2: dto.dimension2 ?? null,\n        },\n      });\n\n      await tx.salesQuotationItem.update({\n        where: { id: quotationItem.id },\n        data: { orderedQty: { increment: diff }, openQty: { decrement: diff } },\n      });\n\n      const updatedQItem = await tx.salesQuotationItem.findUnique({\n        where: { id: quotationItem.id },\n      });\n      if (updatedQItem) {\n        await refreshLineStatus(\n          tx,\n          'salesQuotationItem',\n          quotationItem.id,\n          Number(updatedQItem.openQty),\n        );\n      }\n\n      const pendingInOrder =\n        dto.quantity - Number(line.deliveredQty) - Number(line.invoicedQty);\n      await refreshLineStatus(tx, 'salesOrderItem', lineId, pendingInOrder);\n\n      await this.recalculateTotals(orderId, tenantId, tx);\n\n      await tx.salesOrder.update({\n        where: { id: orderId },\n        data: { updatedById: updatedById ?"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "});\n    if (!line || line.orderId !== orderId)\n      throw new NotFoundException('Línea no encontrada');\n\n    return this.prisma.$transaction(async (tx) => {\n      if (line.quotationItemId) {\n        await tx.salesQuotationItem.update({\n          where: { id: line.quotationItemId },\n          data: {\n            orderedQty: { decrement: Number(line.quantity) },\n            openQty: { increment: Number(line.quantity) },\n          },\n        });\n\n        await clearTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          line.quotationItemId,\n        );\n\n        const updatedQItem = await tx.salesQuotationItem.findUnique({\n          where: { id: line.quotationItemId },\n        });\n        if (updatedQItem) {\n          await refreshLineStatus(\n            tx,\n            'salesQuotationItem',\n            line.quotationItemId,\n            Number(updatedQItem.openQty),\n          );\n        }\n      }\n\n      await tx.salesOrderItem.delete({ where: { id: lineId } });\n      await this.recalculateTotals(orderId"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "       const linkedLines = await tx.salesOrderItem.findMany({\n          where: { orderId: id },\n        });\n        for (const line of linkedLines) {\n          if (line.quotationItemId) {\n            await tx.salesQuotationItem.update({\n              where: { id: line.quotationItemId },\n              data: {\n                orderedQty: { decrement: Number(line.quantity) },\n                openQty: { increment: Number(line.quantity) },\n              },\n            });\n            await clearTargetOnSourceLine(\n              tx,\n              'salesQuotationItem',\n              line.quotationItemId,\n            );\n            const qItem = await tx.salesQuotationItem.findUnique({\n              where: { id: line.quotationItemId },\n            });\n            if (qItem) {\n              const updatedQItemForStatus =\n                await tx.salesQuotationItem.findUnique({\n                  where: { id: qItem.id },\n                });\n              if (updatedQItemForStatus)\n                await refreshLineStatus(\n          "
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "Map.get(warehouseId)!.add(itemId);\n      };\n\n      for (const line of currentLines) {\n        if (!(dto.items ?? []).find((i) => i.id === line.id)) {\n          if (line.quotationItemId) {\n            await tx.salesQuotationItem.update({\n              where: { id: line.quotationItemId },\n              data: {\n                orderedQty: { decrement: Number(line.quantity) },\n                openQty: { increment: Number(line.quantity) },\n              },\n            });\n            await clearTargetOnSourceLine(\n              tx,\n              'salesQuotationItem',\n              line.quotationItemId,\n            );\n            const qItem = await tx.salesQuotationItem.findUnique({\n              where: { id: line.quotationItemId },\n            });\n            if (qItem) {\n              const updatedQItemForStatus =\n                await tx.salesQuotationItem.findUnique({\n                  where: { id: qItem.id },\n                });\n              if (updatedQItemForStatus)\n                await refreshLineStatus(\n          "
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "null,\n          );\n\n          // Actualizar orderedQty en cotización (solo si la línea tiene origen en cotización)\n          if (qItem) {\n            const diff = i.quantity - currentQty;\n            await tx.salesQuotationItem.update({\n              where: { id: qItem.id },\n              data: {\n                orderedQty: { increment: diff },\n                openQty: { decrement: diff },\n              },\n            });\n            const updatedQItem = await tx.salesQuotationItem.findUnique({\n              where: { id: qItem.id },\n            });\n            if (updatedQItem) {\n              await refreshLineStatus(\n                tx,\n                'salesQuotationItem',\n                qItem.id,\n                Number(updatedQItem.openQty),\n              );\n            }\n          }\n\n          await tx.salesOrderItem.update({\n            where: { id: i.id },\n            data: {\n              quantity: i.quantity,\n              price,\n              discountPct:\n                discountMode === 'line' ? (i.discountPc"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "reshLineStatus(\n                tx,\n                'salesQuotationItem',\n                qItem.id,\n                Number(updatedQItem.openQty),\n              );\n            }\n          }\n\n          await tx.salesOrderItem.update({\n            where: { id: i.id },\n            data: {\n              quantity: i.quantity,\n              price,\n              discountPct:\n                discountMode === 'line' ? (i.discountPct ?? null) : null,\n              discountAmt:\n                discountMode === 'line' ? (i.discountAmt ?? null) : null,\n              discountTotal: lineCalc.discountTotal,\n              priceNet: lineCalc.priceNet,\n              subtotal: lineCalc.lineFinal,\n              taxAmount: lineCalc.taxAmount,\n              taxIndicatorId: effectiveTaxIndicatorId,\n              taxRate: effectiveTaxRate,\n              warehouseId: lineWh ?? null,\n              cost: updatedCost,\n              totalCost: updatedCost != null ? updatedCost * i.quantity : null,\n              totalWeight:\n                orderI"
      },
      {
        "type": "create",
        "model": "salesOrderItem",
        "snippet": " getAvgCost(\n            tx,\n            tenantId,\n            itemRecord.id,\n            lineWh ?? null,\n            itemRecord.cost != null ? Number(itemRecord.cost) : null,\n          );\n\n          await tx.salesOrderItem.create({\n            data: {\n              tenantId: tenantId as number,\n              orderId: id,\n              quotationItemId: null, // sin trazabilidad\n              itemId,\n              itemName: itemRecord.name,\n              itemCode: itemRecord.code,\n              quantity: i.quantity,\n              price,\n              discountPct:\n                discountMode === 'line' ? (i.discountPct ?? null) : null,\n              discountAmt:\n                discountMode === 'line' ? (i.discountAmt ?? null) : null,\n              discountTotal: lineCalc.discountTotal,\n              priceNet: lineCalc.priceNet,\n              subtotal: lineCalc.lineFinal,\n              cost: newLineCost,\n              totalCost: newLineCost != null ? newLineCost * i.quantity : null,\n              weight:\n            "
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "      taxIndicatorId: qItem.taxIndicatorId ?? null,\n          });\n\n          addAffected(qItem.itemId, lineWh ?? currentOrder.warehouseId);\n\n          // Actualizar orderedQty en cotización\n          await tx.salesQuotationItem.update({\n            where: { id: qItem.id },\n            data: {\n              orderedQty: { increment: i.quantity },\n              openQty: { decrement: i.quantity },\n            },\n          });\n\n          const newItem = await tx.salesOrderItem.create({\n            data: {\n              tenantId: tenantId as number,\n              orderId: id,\n              quotationItemId: qItem.id,\n              itemId: qItem.itemId,\n              itemName: qItem.item.name,\n              itemCode: qItem.item.code,\n              quantity: i.quantity,\n              price,\n              discountPct:\n                discountMode === 'line' ? (i.discountPct ?? null) : null,\n              discountAmt:\n                discountMode === 'line' ? (i.discountAmt ?? null) : null,\n              discountTotal: lineCalc.di"
      },
      {
        "type": "create",
        "model": "salesOrderItem",
        "snippet": "e: { id: qItem.id },\n            data: {\n              orderedQty: { increment: i.quantity },\n              openQty: { decrement: i.quantity },\n            },\n          });\n\n          const newItem = await tx.salesOrderItem.create({\n            data: {\n              tenantId: tenantId as number,\n              orderId: id,\n              quotationItemId: qItem.id,\n              itemId: qItem.itemId,\n              itemName: qItem.item.name,\n              itemCode: qItem.item.code,\n              quantity: i.quantity,\n              price,\n              discountPct:\n                discountMode === 'line' ? (i.discountPct ?? null) : null,\n              discountAmt:\n                discountMode === 'line' ? (i.discountAmt ?? null) : null,\n              discountTotal: lineCalc.discountTotal,\n              priceNet: lineCalc.priceNet,\n              subtotal: lineCalc.lineFinal,\n              cost: await getAvgCost(\n                tx,\n                tenantId,\n                qItem.itemId,\n                lineWh,\n           "
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "null,\n              dimension1: i.dimension1 ?? null,\n              dimension2: i.dimension2 ?? null,\n            },\n          });\n\n          // Actualizar target* en la línea de cotización\n          await tx.salesQuotationItem.update({\n            where: { id: qItem.id },\n            data: {\n              targetDocType: DocumentType.SALES_ORDER,\n              targetDocId: id,\n              targetLineId: newItem.id,\n            },\n          });\n\n          // Refrescar lineStatus de la línea de cotización\n          const updatedQItem = await tx.salesQuotationItem.findUnique({\n            where: { id: qItem.id },\n          });\n          if (updatedQItem) {\n            await refreshLineStatus(\n              tx,\n              'salesQuotationItem',\n              qItem.id,\n              Number(updatedQItem.openQty),\n            );\n          }\n        }\n      } // ← cierre del for (const i of dto.items)\n\n      // Validar impuesto obligatorio del partner (igual que en create)\n      validateTaxRequired(\n        partner.taxRequir"
      }
    ]
  },
  {
    "file": "delivery-orders/delivery-orders.service.ts",
    "matches": [
      {
        "type": "create",
        "model": "deliveryOrderItem",
        "snippet": "r;\n        qty: number;\n        itemId: number;\n        batchId: number | null;\n        serialNumberId: number | null;\n      }[] = [];\n\n      for (const di of deliveryItems) {\n        const created = await tx.deliveryOrderItem.create({\n          data: { deliveryOrderId: delivery.id, ...di },\n        });\n        createdItems.push({\n          id: created.id,\n          quotationItemId: di.baseLineId as number,\n          qty: Number(di.quantity),\n          itemId: di.itemId,\n          batchId: di.batchId ?? null,\n          serialNumberId: di.serialNumberId ?? null,\n        });\n      }\n\n      await linkDocuments(\n        tx,\n        tenantId,\n        DocumentType.SALES_QUOTATION,\n        quotationId,\n        DocumentType.DELIVERY_ORDER,\n        delivery.id,\n      );\n\n      const whItemMap = new Map<number, Set<number>>();\n\n      for (const ci of createdItems) {\n        const qi = quotation.items.find((i) => i.id === ci.quotationItemId)!;\n        const lineWh =\n          payload.items.find((p) => p.quotationItemId === ci.quo"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "t setTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          ci.quotationItemId,\n          DocumentType.DELIVERY_ORDER,\n          delivery.id,\n          ci.id,\n        );\n\n        await tx.salesQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n        const updatedQI = await tx.salesQuotationItem.findUnique({\n          where: { id: ci.quotationItemId },\n        });\n        if (updatedQI)\n          await refreshLineStatus(\n            tx,\n            'salesQuotationItem',\n            ci.quotationItemId,\n            Number(updatedQI.openQty),\n          );\n\n        await refreshLineStatus(tx, 'deliveryOrderItem', ci.id, 0);\n\n        if (lineWh) {\n          // ✅ MEJORA: propagar batch/serie a StockMovement\n          await tx.stockMovement.create({\n            data: {\n              tenantId,\n              itemId: ci.itemId,\n              warehouseId: lineW"
      },
      {
        "type": "create",
        "model": "deliveryOrderItem",
        "snippet": "taxAmount ?? 0) * ratio;\n        const lineNet = Math.round((lineFinal - lineTaxAmount) * 100) / 100;\n        const lineDiscountTotal = Number(ii.discountTotal ?? 0) * ratio;\n\n        const created = await tx.deliveryOrderItem.create({\n          data: {\n            tenantId,\n            deliveryOrderId: delivery.id,\n            orderItemId,\n            itemId: ii.itemId,\n            itemName: ii.item.name,\n            itemCode: ii.item.code,\n            quantity: line.quantity,\n            price: ii.price != null ? Number(ii.price) : null,\n            priceNet: ii.priceNet != null ? Number(ii.priceNet) : null,\n            discountPct: ii.discountPct != null ? Number(ii.discountPct) : null,\n            discountAmt: ii.discountAmt != null ? Number(ii.discountAmt) : null,\n            discountTotal: lineDiscountTotal,\n            subtotal: lineNet,\n            lineNet: lineNet,\n            lineFinal: lineFinal,\n            taxIndicatorId: ii.taxIndicatorId ?? null,\n            taxRate: ii.taxRate != null ? Number(ii.taxRat"
      },
      {
        "type": "update",
        "model": "saleInvoiceItem",
        "snippet": "setTargetOnSourceLine(\n          tx,\n          'saleInvoiceItem',\n          ci.reserveInvoiceItemId,\n          DocumentType.DELIVERY_ORDER,\n          delivery.id,\n          ci.id,\n        );\n\n        await tx.saleInvoiceItem.update({\n          where: { id: ci.reserveInvoiceItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n        const updatedII = await tx.saleInvoiceItem.findUnique({\n          where: { id: ci.reserveInvoiceItemId },\n        });\n        if (updatedII)\n          await refreshLineStatus(\n            tx,\n            'saleInvoiceItem',\n            ci.reserveInvoiceItemId,\n            Number(updatedII.openQty),\n          );\n\n        await refreshLineStatus(tx, 'deliveryOrderItem', ci.id, 0);\n\n        if (lineWh) {\n          // ✅ MEJORA: propagar batch/serie a StockMovement\n          await tx.stockMovement.create({\n            data: {\n              tenantId,\n              itemId: ci.itemId,\n              warehouseId: lineWh,\n              batchId: ci.batchId,\n              serialNum"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "eliveryOrderTotals(tx, delivery.id);\n\n      // Actualizar deliveredQty en salesOrderItem para cada línea con orderItemId\n      for (const ci of createdItems) {\n        if (ci.orderItemId) {\n          await tx.salesOrderItem.update({\n            where: { id: ci.orderItemId },\n            data: {\n              deliveredQty: { increment: ci.qty },\n              openQty: { decrement: ci.qty },\n            },\n          });\n        }\n\n        // La entrega nace desde una FRV → ya está facturada al 100%\n        await tx.deliveryOrderItem.update({\n          where: { id: ci.id },\n          data: { invoicedQty: ci.qty },\n        });\n      }\n\n      // invoiceStatus de la entrega = FULL (viene de FRV)\n      await recalcDeliveryOrderProgress(tx, delivery.id);\n\n      // Recalcular progreso del pedido\n      if (frvOrderId) {\n        await refreshHeaderStatus(\n          tx,\n          'salesOrder',\n          'salesOrderItem',\n          frvOrderId,\n          'OPEN',\n          'CLOSED',\n        );\n        await recalcSalesOrderProgres"
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "deliveredQty: { increment: ci.qty },\n              openQty: { decrement: ci.qty },\n            },\n          });\n        }\n\n        // La entrega nace desde una FRV → ya está facturada al 100%\n        await tx.deliveryOrderItem.update({\n          where: { id: ci.id },\n          data: { invoicedQty: ci.qty },\n        });\n      }\n\n      // invoiceStatus de la entrega = FULL (viene de FRV)\n      await recalcDeliveryOrderProgress(tx, delivery.id);\n\n      // Recalcular progreso del pedido\n      if (frvOrderId) {\n        await refreshHeaderStatus(\n          tx,\n          'salesOrder',\n          'salesOrderItem',\n          frvOrderId,\n          'OPEN',\n          'CLOSED',\n        );\n        await recalcSalesOrderProgress(tx, frvOrderId);\n      }\n\n      for (const [wId, ids] of whItemMap) {\n        for (const itemId of ids)\n          await recalcCommitted(tx, tenantId, itemId, wId);\n      }\n\n      return this.findOneTx(tx, delivery.id);\n    });\n  }\n\n  // ======================================================\n  // Draft consolid"
      },
      {
        "type": "create",
        "model": "deliveryOrderItem",
        "snippet": "mId,\n          lineWh,\n          ii.item.cost != null ? Number(ii.item.cost) : null,\n        );\n        const weight = ii.item.weight != null ? Number(ii.item.weight) : null;\n\n        const created = await tx.deliveryOrderItem.create({\n          data: {\n            tenantId,\n            deliveryOrderId: delivery.id,\n            itemId: ii.itemId,\n            itemName: ii.item.name,\n            itemCode: ii.item.code,\n            quantity: line.quantity,\n            price: ii.price != null ? Number(ii.price) : null,\n            priceNet: ii.priceNet != null ? Number(ii.priceNet) : null,\n            discountPct: ii.discountPct != null ? Number(ii.discountPct) : null,\n            discountAmt: ii.discountAmt != null ? Number(ii.discountAmt) : null,\n            discountTotal:\n              ii.discountTotal != null ? Number(ii.discountTotal) : 0,\n            cost,\n            totalCost: cost != null ? cost * line.quantity : null,\n            weight,\n            totalWeight: weight != null ? weight * line.quantity : null,\n   "
      },
      {
        "type": "update",
        "model": "saleInvoiceItem",
        "snippet": " setTargetOnSourceLine(\n          tx,\n          'saleInvoiceItem',\n          ci.reserveInvoiceItemId,\n          DocumentType.DELIVERY_ORDER,\n          delivery.id,\n          ci.id,\n        );\n        await tx.saleInvoiceItem.update({\n          where: { id: ci.reserveInvoiceItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n        const updatedII = await tx.saleInvoiceItem.findUnique({\n          where: { id: ci.reserveInvoiceItemId },\n        });\n        if (updatedII)\n          await refreshLineStatus(\n            tx,\n            'saleInvoiceItem',\n            ci.reserveInvoiceItemId,\n            Number(updatedII.openQty),\n          );\n        await refreshLineStatus(tx, 'deliveryOrderItem', ci.id, 0);\n\n        if (ci.lineWh) {\n          // ✅ MEJORA: propagar batch/serie a StockMovement\n          await tx.stockMovement.create({\n            data: {\n              tenantId,\n              itemId: ci.itemId,\n              warehouseId: ci.lineWh,\n              batchId: ci.batchId ?? null,\n          "
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "elivery.id },\n      });\n      await this.recalculateDeliveryOrderTotals(tx, delivery.id);\n\n      // La entrega nace desde FRVs → ya está facturada al 100%\n      for (const item of allItems) {\n        await tx.deliveryOrderItem.update({\n          where: { id: item.id },\n          data: { invoicedQty: item.quantity },\n        });\n      }\n      await recalcDeliveryOrderProgress(tx, delivery.id);\n\n      for (const [wId, ids] of whItemMap)\n        for (const itemId of ids)\n          await recalcCommitted(tx, tenantId, itemId, wId);\n\n      return delivery.id;\n    });\n\n    return this.findOne(deliveryId);\n  }\n\n  // ======================================================\n  // Crear entrega desde pedido — persiste en OPEN\n  // ======================================================\n  async createFromOrder(\n    tenantId: number,\n    orderId: number,\n    payload: {\n      notes?: string;\n      date?: string;\n      postingDate?: string;\n      deliveryDate?: string | null;\n      warehouseId?: number | null;\n      items: {\n        orde"
      },
      {
        "type": "create",
        "model": "deliveryOrderItem",
        "snippet": "scountTotal = Number(oi.discountTotal ?? 0) * ratio;\n\n        headerSubtotal += lineNet;\n        headerTax += lineTaxAmount;\n        headerTotalDiscount += lineDiscountTotal;\n\n        const created = await tx.deliveryOrderItem.create({\n          data: {\n            tenantId,\n            deliveryOrderId: delivery.id,\n            orderItemId: line.orderItemId,\n            itemId: oi.itemId,\n            itemName: oi.item.name,\n            itemCode: oi.item.code,\n            quantity: line.quantity,\n            price: oi.price != null ? Number(oi.price) : null,\n            priceNet: oi.priceNet != null ? Number(oi.priceNet) : null,\n            discountPct: oi.discountPct != null ? Number(oi.discountPct) : null,\n            discountAmt: oi.discountAmt != null ? Number(oi.discountAmt) : null,\n            discountTotal: lineDiscountTotal,\n            subtotal: lineNet,\n            lineNet: lineNet,\n            lineFinal: lineFinal,\n            taxIndicatorId: oi.taxIndicatorId ?? null,\n            taxRate: oi.taxRate != null "
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "    ci.orderItemId,\n          DocumentType.DELIVERY_ORDER,\n          delivery.id,\n          ci.id,\n        );\n\n        // Incrementar deliveredQty + decrementar openQty en la línea del pedido\n        await tx.salesOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: {\n            deliveredQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n\n        // Recalcular lineStatus usando openQty actualizado\n        const updatedOI = await tx.salesOrderItem.findUnique({\n          where: { id: ci.orderItemId },\n        });\n        if (updatedOI) {\n          await refreshLineStatus(\n            tx,\n            'salesOrderItem',\n            ci.orderItemId,\n            Number(updatedOI.openQty),\n          );\n        }\n\n        // Línea de entrega nace CLOSED → lineStatus = CLOSED\n        await refreshLineStatus(tx, 'deliveryOrderItem', ci.id, 0);\n\n        // Descontar stock físico + crear movimiento\n        if (lineWh) {\n          // ✅ MEJORA: propagar batch/ser"
      },
      {
        "type": "create",
        "model": "deliveryOrderItem",
        "snippet": "  total: headerTotal,\n          totalDiscount: 0,\n          totalCost: headerTotalCost,\n          totalWeight: headerTotalWeight,\n        },\n      });\n\n      for (const di of deliveryItems) {\n        await tx.deliveryOrderItem.create({\n          data: { deliveryOrderId: delivery.id, ...di },\n        });\n      }\n\n      // Stock committed\n      const whItemMap = new Map<number, Set<number>>();\n      for (const di of deliveryItems) {\n        if (di.warehouseId) {\n          if (!whItemMap.has(di.warehouseId))\n            whItemMap.set(di.warehouseId, new Set());\n          whItemMap.get(di.warehouseId)!.add(di.itemId);\n        }\n      }\n      for (const [wId, ids] of whItemMap) {\n        for (const itemId of ids)\n          await recalcCommitted(tx, tenantId, itemId, wId);\n      }\n\n      // Trazabilidad: vincular con cotización origen si viene de \"Copiar a\"\n      if (payload.salesQuotationId) {\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_QUOTATION,\n          payload.salesQuotat"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": ">>();\n      const stockLines: any[] = [];\n\n      for (const line of items) {\n        // Incrementar deliveredQty + decrementar openQty en la línea del pedido\n        if (line.orderItemId) {\n          await tx.salesOrderItem.update({\n            where: { id: line.orderItemId },\n            data: {\n              deliveredQty: { increment: Number(line.quantity) },\n              openQty: { decrement: Number(line.quantity) },\n            },\n          });\n\n          // Recalcular lineStatus en la línea del pedido\n          const updatedOI = await tx.salesOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (updatedOI) {\n            await refreshLineStatus(\n              tx,\n              'salesOrderItem',\n              line.orderItemId,\n              Number(updatedOI.openQty),\n            );\n          }\n        }\n\n        // Actualizar openQty de la línea de entrega a 0 (entregada) y marcar CLOSED\n        await tx.deliveryOrderItem.update({\n          where: { id: line.id },\n      "
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "          line.orderItemId,\n              Number(updatedOI.openQty),\n            );\n          }\n        }\n\n        // Actualizar openQty de la línea de entrega a 0 (entregada) y marcar CLOSED\n        await tx.deliveryOrderItem.update({\n          where: { id: line.id },\n          data: { openQty: 0 },\n        });\n        await refreshLineStatus(tx, 'deliveryOrderItem', line.id, 0);\n\n        const lineWarehouseId = line.warehouseId ?? headerWarehouseId;\n        if (!lineWarehouseId)\n          throw new BadRequestException(\n            `La línea del artículo ${line.itemId} no tiene almacén`,\n          );\n\n        const unitCost = await getAvgCost(\n          tx,\n          tenantId,\n          line.itemId,\n          lineWarehouseId,\n          line.cost != null ? Number(line.cost) : null,\n        );\n\n        stockLines.push({\n          itemId: line.itemId,\n          quantity: line.quantity,\n          warehouseId: lineWarehouseId,\n          unitCost,\n          batchId: line.batchId ?? null,\n          serialNumberId: line.seria"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": ">();\n      const stockLines: any[] = [];\n\n      for (const line of items) {\n        // ── Revertir deliveredQty + restaurar openQty en la línea del pedido ──\n        if (line.orderItemId) {\n          await tx.salesOrderItem.update({\n            where: { id: line.orderItemId },\n            data: {\n              deliveredQty: { decrement: Number(line.quantity) },\n              openQty: { increment: Number(line.quantity) },\n            },\n          });\n\n          await clearTargetOnSourceLine(tx, 'salesOrderItem', line.orderItemId);\n\n          const oi = await tx.salesOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (oi) {\n            await refreshLineStatus(\n              tx,\n              'salesOrderItem',\n              line.orderItemId,\n              Number(oi.openQty),\n            );\n          }\n        }\n\n        // ── Restaurar openQty en la línea de entrega ──────────────────────\n        await tx.deliveryOrderItem.update({\n          where: { id: line.id },\n          "
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "derItem',\n              line.orderItemId,\n              Number(oi.openQty),\n            );\n          }\n        }\n\n        // ── Restaurar openQty en la línea de entrega ──────────────────────\n        await tx.deliveryOrderItem.update({\n          where: { id: line.id },\n          data: { openQty: Number(line.quantity) },\n        });\n\n        // ── Revertir stock físico (toda entrega tiene stock descontado) ──\n        const lineWh = line.warehouseId ?? delivery.warehouseId;\n        if (lineWh) {\n          await tx.stockMovement.deleteMany({\n            where: {\n              deliveryOrderId: deliveryId,\n              itemId: line.itemId,\n              warehouseId: lineWh,\n              type: StockMovementType.SALE_DELIVERY,\n            },\n          });\n\n          stockLines.push({\n            itemId: line.itemId,\n            quantity: line.quantity,\n            warehouseId: lineWh,\n            unitCost: null,\n            batchId: line.batchId ?? null,\n            serialNumberId: line.serialNumberId ?? null,\n            t"
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "al) * subtotal;\n              }\n            }\n\n            const totalCost = cost != null ? cost * newQty : null;\n            const totalWeight = weight != null ? weight * newQty : null;\n\n            await tx.deliveryOrderItem.update({\n              where: { id: line.id },\n              data: {\n                quantity: newQty,\n                warehouseId: effectiveWh ?? null,\n                customFields: line.customFields ?? existing.customFields ?? {},\n                subtotal,\n                taxIndicatorId: taxIndicatorId ?? null,\n                taxRate,\n                taxAmount,\n                totalCost,\n                totalWeight,\n                openQty: newQty,\n                batchId:\n                  line.batchId !== undefined ? line.batchId : existing.batchId,\n                serialNumberId:\n                  line.serialNumberId !== undefined\n                    ? line.serialNumberId\n                    : existing.serialNumberId,\n              },\n            });\n\n            headerSubtotal += subtotal;"
      },
      {
        "type": "create",
        "model": "deliveryOrderItem",
        "snippet": "                 : 0;\n              }\n            }\n\n            const totalCost = cost != null ? cost * qty : null;\n            const totalWeight = weight != null ? weight * qty : null;\n\n            await tx.deliveryOrderItem.create({\n              data: {\n                tenantId,\n                deliveryOrderId: deliveryId,\n                itemId: item.id,\n                itemName: item.name,\n                itemCode: item.code,\n                quantity: qty,\n                price,\n                priceNet: line.priceNet != null ? Number(line.priceNet) : price,\n                discountPct: line.discountPct ?? null,\n                discountAmt: line.discountAmt ?? null,\n                discountTotal: line.discountTotal ?? 0,\n                subtotal,\n                taxIndicatorId,\n                taxRate,\n                taxAmount,\n                cost,\n                totalCost,\n                weight,\n                totalWeight,\n                warehouseId: effectiveWh,\n                lineNum: index,\n           "
      },
      {
        "type": "create",
        "model": "deliveryOrderItem",
        "snippet": "mId,\n          lineWh,\n          oi.item.cost != null ? Number(oi.item.cost) : null,\n        );\n        const weight = oi.item.weight != null ? Number(oi.item.weight) : null;\n\n        const created = await tx.deliveryOrderItem.create({\n          data: {\n            tenantId,\n            deliveryOrderId: delivery.id,\n            orderItemId: line.orderItemId,\n            itemId: oi.itemId,\n            itemName: oi.item.name,\n            itemCode: oi.item.code,\n            quantity: line.quantity,\n            price: oi.price != null ? Number(oi.price) : null,\n            priceNet: oi.priceNet != null ? Number(oi.priceNet) : null,\n            discountPct: oi.discountPct != null ? Number(oi.discountPct) : null,\n            discountAmt: oi.discountAmt != null ? Number(oi.discountAmt) : null,\n            discountTotal:\n              oi.discountTotal != null ? Number(oi.discountTotal) : 0,\n            cost: unitCost,\n            totalCost: unitCost != null ? unitCost * line.quantity : null,\n            weight,\n            tot"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "     await setTargetOnSourceLine(\n          tx,\n          'salesOrderItem',\n          ci.orderItemId,\n          DocumentType.DELIVERY_ORDER,\n          delivery.id,\n          ci.id,\n        );\n        await tx.salesOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: {\n            deliveredQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n        const updatedOI = await tx.salesOrderItem.findUnique({\n          where: { id: ci.orderItemId },\n        });\n        if (updatedOI)\n          await refreshLineStatus(\n            tx,\n            'salesOrderItem',\n            ci.orderItemId,\n            Number(updatedOI.openQty),\n          );\n        await refreshLineStatus(tx, 'deliveryOrderItem', ci.id, 0);\n\n        if (ci.lineWh) {\n          // ✅ MEJORA: propagar batch/serie a StockMovement\n          await tx.stockMovement.create({\n            data: {\n              tenantId,\n              itemId: ci.itemId,\n              warehouseId: ci.lineWh,\n          "
      },
      {
        "type": "create",
        "model": "deliveryOrderItem",
        "snippet": "mId,\n          lineWh,\n          qi.item.cost != null ? Number(qi.item.cost) : null,\n        );\n        const weight = qi.item.weight != null ? Number(qi.item.weight) : null;\n\n        const created = await tx.deliveryOrderItem.create({\n          data: {\n            tenantId,\n            deliveryOrderId: delivery.id,\n            itemId: qi.itemId,\n            itemName: qi.item.name,\n            itemCode: qi.item.code,\n            quantity: line.quantity,\n            price: qi.price != null ? Number(qi.price) : null,\n            priceNet: qi.priceNet != null ? Number(qi.priceNet) : null,\n            discountPct: qi.discountPct != null ? Number(qi.discountPct) : null,\n            discountAmt: qi.discountAmt != null ? Number(qi.discountAmt) : null,\n            discountTotal:\n              qi.discountTotal != null ? Number(qi.discountTotal) : 0,\n            cost,\n            totalCost: cost != null ? cost * line.quantity : null,\n            weight,\n            totalWeight: weight != null ? weight * line.quantity : null,\n   "
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "it setTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          ci.quotationItemId,\n          DocumentType.DELIVERY_ORDER,\n          delivery.id,\n          ci.id,\n        );\n        await tx.salesQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n        const updatedQI = await tx.salesQuotationItem.findUnique({\n          where: { id: ci.quotationItemId },\n        });\n        if (updatedQI)\n          await refreshLineStatus(\n            tx,\n            'salesQuotationItem',\n            ci.quotationItemId,\n            Number(updatedQI.openQty),\n          );\n        await refreshLineStatus(tx, 'deliveryOrderItem', ci.id, 0);\n\n        if (ci.lineWh) {\n          // ✅ MEJORA: propagar batch/serie a StockMovement\n          await tx.stockMovement.create({\n            data: {\n              tenantId,\n              itemId: ci.itemId,\n              warehouseId: ci."
      }
    ]
  },
  {
    "file": "sale-reserve-invoices/sale-reserve-invoices.service.ts",
    "matches": [
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": " },\n      });\n\n      const createdItems: {\n        id: number;\n        deliveryOrderItemId: number;\n        qty: number;\n      }[] = [];\n      for (const il of invoiceLines) {\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...il },\n        });\n        createdItems.push({\n          id: created.id,\n          deliveryOrderItemId: il.deliveryOrderItemId!,\n          qty: Number(il.quantity),\n        });\n      }\n\n      await linkDocuments(\n        tx,\n        tenantId,\n        DocumentType.DELIVERY_ORDER,\n        deliveryOrderId,\n        DocumentType.SALE_RESERVE_INVOICE,\n        invoice.id,\n      );\n\n      if (effectiveOrderId)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_ORDER,\n          effectiveOrderId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n        );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'deliveryOrderItem',\n          ci.deliv"
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "Id,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n          ci.id,\n        );\n        // ✅ NUEVO: incrementar invoicedQty en DeliveryOrderItem y propagar a SalesOrderItem\n        await tx.deliveryOrderItem.update({\n          where: { id: ci.deliveryOrderItemId },\n          data: { invoicedQty: { increment: ci.qty } },\n        });\n        const updatedDI = await tx.deliveryOrderItem.findUnique({\n          where: { id: ci.deliveryOrderItemId },\n        });\n        if (updatedDI) {\n          const orderItemId = updatedDI.orderItemId;\n          if (orderItemId) {\n            await tx.salesOrderItem.update({\n              where: { id: orderItemId },\n              data: { invoicedQty: { increment: ci.qty } },\n            });\n            const updatedOI = await tx.salesOrderItem.findUnique({\n              where: { id: orderItemId },\n            });\n            if (updatedOI) {\n              const pendingOI =\n                Number(updatedOI.quantity) - Number(updatedOI.invoicedQty ?? 0);\n              awa"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "erItem.findUnique({\n          where: { id: ci.deliveryOrderItemId },\n        });\n        if (updatedDI) {\n          const orderItemId = updatedDI.orderItemId;\n          if (orderItemId) {\n            await tx.salesOrderItem.update({\n              where: { id: orderItemId },\n              data: { invoicedQty: { increment: ci.qty } },\n            });\n            const updatedOI = await tx.salesOrderItem.findUnique({\n              where: { id: orderItemId },\n            });\n            if (updatedOI) {\n              const pendingOI =\n                Number(updatedOI.quantity) - Number(updatedOI.invoicedQty ?? 0);\n              await refreshLineStatus(\n                tx,\n                'salesOrderItem',\n                orderItemId,\n                pendingOI,\n              );\n            }\n          }\n        }\n      }\n\n      // Recalcular invoiceStatus del pedido origen\n      if (effectiveOrderId)\n        await recalcSalesOrderProgress(tx, effectiveOrderId);\n\n      return this.findOneTx(tx, invoice.id);\n    });\n  }\n\n "
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "     id: number;\n        quotationItemId: number;\n        qty: number;\n      }[] = [];\n      for (const ii of invoiceItems) {\n        const { _quotationItemId, ...data } = ii;\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...data },\n        });\n        createdItems.push({\n          id: created.id,\n          quotationItemId: _quotationItemId!,\n          qty: Number(ii.quantity),\n        });\n      }\n\n      await linkDocuments(\n        tx,\n        tenantId,\n        DocumentType.SALES_QUOTATION,\n        quotationId,\n        DocumentType.SALE_RESERVE_INVOICE,\n        invoice.id,\n      );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          ci.quotationItemId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { inc"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "tTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          ci.quotationItemId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n        const updatedQi = await tx.salesQuotationItem.findUnique({\n          where: { id: ci.quotationItemId },\n        });\n        if (updatedQi) {\n          const pending = Number(\n            updatedQi.openQty ??\n              Number(updatedQi.quantity) - Number(updatedQi.orderedQty),\n          );\n          await refreshLineStatus(\n            tx,\n            'salesQuotationItem',\n            ci.quotationItemId,\n            pending,\n          );\n        }\n      }\n\n      const allQItems = await tx.salesQuotationItem.findMany({\n        where: { quotationId },\n      });\n      if (allQItems.every((i) => Number(i."
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "      const createdItems: { id: number; orderItemId: number; qty: number }[] =\n        [];\n      for (const ii of invoiceItems) {\n        const { _orderItemId, ...data } = ii;\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...data },\n        });\n        createdItems.push({\n          id: created.id,\n          orderItemId: _orderItemId!,\n          qty: Number(ii.quantity),\n        });\n      }\n\n      await linkDocuments(\n        tx,\n        tenantId,\n        DocumentType.SALES_ORDER,\n        orderId,\n        DocumentType.SALE_RESERVE_INVOICE,\n        invoice.id,\n      );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesOrderItem',\n          ci.orderItemId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n          ci.id,\n        );\n        const updatedOi = await tx.salesOrderItem.findUnique({\n          where: { id: ci.orderItemId },\n        });\n        if (updatedOi) {\n          const pend"
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": ", 'totalWeight'),\n          currency: payload.currency ?? 'BOB',\n          exchangeRate,\n          createdById: createdById ?? null,\n        },\n      });\n\n      for (const ii of invoiceItems)\n        await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...ii },\n        });\n\n      if (payload.salesQuotationId)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_QUOTATION,\n          payload.salesQuotationId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n        );\n      if (payload.salesOrderId)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_ORDER,\n          payload.salesOrderId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n        );\n\n      const createdLines = await tx.saleInvoiceItem.findMany({\n        where: { saleInvoiceId: invoice.id },\n      });\n      for (const ci of createdLines) {\n        if (ci.baseLineId && ci.baseDocType) {\n          const tableName =\n   "
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": " Recopilar orderItemIds únicos para recalcular progreso al final\n      const affectedOrderIds = new Set<number>();\n\n      for (const line of items) {\n        if (line.deliveryOrderItemId) {\n          await tx.deliveryOrderItem.update({\n            where: { id: line.deliveryOrderItemId },\n            data: { invoicedQty: { increment: Number(line.quantity) } },\n          });\n          await refreshLineStatus(tx, 'saleInvoiceItem', line.id, 0);\n          const updatedDI = await tx.deliveryOrderItem.findUnique({\n            where: { id: line.deliveryOrderItemId },\n          });\n          if (updatedDI) {\n            // Propagar invoicedQty al SalesOrderItem si la entrega tiene línea de pedido\n            const orderItemId = updatedDI.orderItemId;\n            if (orderItemId) {\n              await tx.salesOrderItem.update({\n                where: { id: orderItemId },\n                data: { invoicedQty: { increment: Number(line.quantity) } },\n              });\n              const updatedOI = await tx.salesOrderItem.findUniq"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "atedDI) {\n            // Propagar invoicedQty al SalesOrderItem si la entrega tiene línea de pedido\n            const orderItemId = updatedDI.orderItemId;\n            if (orderItemId) {\n              await tx.salesOrderItem.update({\n                where: { id: orderItemId },\n                data: { invoicedQty: { increment: Number(line.quantity) } },\n              });\n              const updatedOI = await tx.salesOrderItem.findUnique({\n                where: { id: orderItemId },\n              });\n              if (updatedOI) {\n                const pendingOI =\n                  Number(updatedOI.quantity) -\n                  Number(updatedOI.invoicedQty ?? 0);\n                await refreshLineStatus(\n                  tx,\n                  'salesOrderItem',\n                  orderItemId,\n                  pendingOI,\n                );\n                if (updatedOI.orderId) affectedOrderIds.add(updatedOI.orderId);\n              }\n            }\n          }\n        } else {\n          await refreshLineStatus(tx, 'saleIn"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "       if (di) {\n            // Revertir invoicedQty en SalesOrderItem si la entrega tiene línea de pedido\n            const orderItemId = di.orderItemId;\n            if (orderItemId) {\n              await tx.salesOrderItem.update({\n                where: { id: orderItemId },\n                data: { invoicedQty: { decrement: Number(line.quantity) } },\n              });\n              const updatedOI = await tx.salesOrderItem.findUnique({\n                where: { id: orderItemId },\n              });\n              if (updatedOI) {\n                const pendingOI =\n                  Number(updatedOI.quantity) -\n                  Number(updatedOI.invoicedQty ?? 0);\n                await refreshLineStatus(\n                  tx,\n                  'salesOrderItem',\n                  orderItemId,\n                  pendingOI,\n                );\n                if (updatedOI.orderId) cancelOrderIds.add(updatedOI.orderId);\n              }\n            }\n          }\n        }\n\n        if (\n          line.baseDocType === DocumentT"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "dd(updatedOI.orderId);\n              }\n            }\n          }\n        }\n\n        if (\n          line.baseDocType === DocumentType.SALES_QUOTATION &&\n          line.baseLineId\n        ) {\n          await tx.salesQuotationItem.update({\n            where: { id: line.baseLineId },\n            data: { orderedQty: { decrement: Number(line.quantity) } },\n          });\n          const updatedQi = await tx.salesQuotationItem.findUnique({\n            where: { id: line.baseLineId },\n          });\n          if (updatedQi) {\n            const pending =\n              Number(updatedQi.quantity) - Number(updatedQi.orderedQty);\n            await refreshLineStatus(\n              tx,\n              'salesQuotationItem',\n              line.baseLineId,\n              pending,\n            );\n            await tx.salesQuotation.updateMany({\n              where: { id: updatedQi.quotationId, status: 'CLOSED' },\n              data: { status: 'OPEN' },\n            });\n          }\n        }\n\n        if (line.baseDocType === DocumentType.SALES_ORD"
      },
      {
        "type": "update",
        "model": "saleInvoiceItem",
        "snippet": "sting.price),\n            quantity: line.quantity,\n            invoiced: true,\n            discountPct,\n            discountAmt,\n            taxRate,\n            isInclusive,\n          });\n\n          await tx.saleInvoiceItem.update({\n            where: { id: line.id },\n            data: {\n              quantity: line.quantity,\n              subtotal: lc.lineFinal,\n              openQty: line.quantity,\n              priceNet: lc.priceNet,\n              discountTotal: lc.discountTotal,\n              taxIndicatorId,\n              taxRate,\n              taxAmount: lc.taxAmount,\n              totalCost:\n                existing.cost != null\n                  ? Number(existing.cost) * line.quantity\n                  : null,\n              totalWeight:\n                existing.weight != null\n                  ? Number(existing.weight) * line.quantity\n                  : null,\n              ...(line.discountPct !== undefined\n                ? { discountPct: line.discountPct }\n                : {}),\n              ...(line.disc"
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "     id: number;\n        quotationItemId: number;\n        qty: number;\n      }[] = [];\n      for (const ii of invoiceItems) {\n        const { _quotationItemId, ...data } = ii;\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...data },\n        });\n        createdItems.push({\n          id: created.id,\n          quotationItemId: _quotationItemId!,\n          qty: Number(ii.quantity),\n        });\n      }\n\n      for (const quotationId of quotationIds)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_QUOTATION,\n          quotationId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n        );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          ci.quotationItemId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesQuotationItem.update({\n          where: { id: ci.quot"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "tTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          ci.quotationItemId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n        const updatedQi = await tx.salesQuotationItem.findUnique({\n          where: { id: ci.quotationItemId },\n        });\n        if (updatedQi)\n          await refreshLineStatus(\n            tx,\n            'salesQuotationItem',\n            ci.quotationItemId,\n            Number(updatedQi.openQty),\n          );\n      }\n\n      for (const quotationId of quotationIds) {\n        const allQI = await tx.salesQuotationItem.findMany({\n          where: { quotationId },\n        });\n        if (allQI.every((i) => Number(i.openQty) <= 0))\n          await tx.salesQuotation.update({\n            where: { id: quotationId },\n"
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "      const createdItems: { id: number; orderItemId: number; qty: number }[] =\n        [];\n      for (const ii of invoiceItems) {\n        const { _orderItemId, ...data } = ii;\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...data },\n        });\n        createdItems.push({\n          id: created.id,\n          orderItemId: _orderItemId!,\n          qty: Number(ii.quantity),\n        });\n      }\n\n      for (const orderId of orderIds)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_ORDER,\n          orderId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n        );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesOrderItem',\n          ci.orderItemId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n          ci.id,\n        );\n        const updatedOi = await tx.salesOrderItem.findUnique({\n          where: { id: ci.orderItemId },\n   "
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "reatedItems: {\n        id: number;\n        deliveryOrderItemId: number;\n        deliveryId: number;\n        qty: number;\n      }[] = [];\n      for (const ii of invoiceItems) {\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...ii },\n        });\n        const linePayload = payload.items.find(\n          (l) => l.deliveryOrderItemId === ii.deliveryOrderItemId,\n        )!;\n        createdItems.push({\n          id: created.id,\n          deliveryOrderItemId: ii.deliveryOrderItemId!,\n          deliveryId: linePayload.deliveryId,\n          qty: Number(ii.quantity),\n        });\n      }\n\n      for (const deliveryId of deliveryIds)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.DELIVERY_ORDER,\n          deliveryId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n        );\n\n      // Linkar también al pedido de venta origen si las entregas pertenecen a un pedido\n      const orderIds = [\n        ...new Set(deliveries.m"
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "rgetOnSourceLine(\n          tx,\n          'deliveryOrderItem',\n          ci.deliveryOrderItemId,\n          DocumentType.SALE_RESERVE_INVOICE,\n          invoice.id,\n          ci.id,\n        );\n        await tx.deliveryOrderItem.update({\n          where: { id: ci.deliveryOrderItemId },\n          data: { invoicedQty: { increment: ci.qty } },\n        });\n        const updatedDI = await tx.deliveryOrderItem.findUnique({\n          where: { id: ci.deliveryOrderItemId },\n        });\n        if (updatedDI) {\n          const pending =\n            Number(updatedDI.quantity) - Number(updatedDI.invoicedQty ?? 0);\n          await refreshLineStatus(\n            tx,\n            'deliveryOrderItem',\n            ci.deliveryOrderItemId,\n            pending,\n          );\n        }\n      }\n\n      // Recalcular progreso de facturación en los pedidos origen\n      for (const orderId of orderIds)\n        await recalcSalesOrderProgress(tx, orderId);\n\n      return this.findOneTx(tx, invoice.id);\n    });\n  }\n\n  // ================================"
      }
    ]
  },
  {
    "file": "sale-invoices/sale-invoices.service.ts",
    "matches": [
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "     quotationItemId: number;\n        qty: number;\n      }[] = [];\n      for (const ii of invoiceItems) {\n        const { _quotationItemId, _isInclusive: _inc, ...data } = ii;\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...data },\n        });\n        createdItems.push({\n          id: created.id,\n          quotationItemId: _quotationItemId!,\n          qty: ii.quantity,\n        });\n      }\n\n      await linkDocuments(\n        tx,\n        tenantId,\n        DocumentType.SALES_QUOTATION,\n        quotationId,\n        docType,\n        invoice.id,\n      );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          ci.quotationItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty }"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "dItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          ci.quotationItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n        const updatedQi = await tx.salesQuotationItem.findUnique({\n          where: { id: ci.quotationItemId },\n        });\n        if (updatedQi)\n          await refreshLineStatus(\n            tx,\n            'salesQuotationItem',\n            ci.quotationItemId,\n            Number(updatedQi.openQty),\n          );\n      }\n\n      await refreshHeaderStatus(\n        tx,\n        'salesQuotation',\n        'salesQuotationItem',\n        quotationId,\n        'OPEN',\n        'CLOSED',\n      );\n\n      return invoice.id;\n    });\n    if (isIns === IS_CLIENT || isIns === IS_RESERVE)\n      return this._autoConfirmReserve(crea"
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "createdItems: { id: number; orderItemId: number; qty: number }[] =\n        [];\n      for (const ii of invoiceItems) {\n        const { _isInclusive: _inc1, ...lineData1 } = ii;\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...lineData1 },\n        });\n        createdItems.push({\n          id: created.id,\n          orderItemId: ii.orderItemId!,\n          qty: ii.quantity,\n        });\n      }\n\n      await linkDocuments(\n        tx,\n        tenantId,\n        DocumentType.SALES_ORDER,\n        orderId,\n        docType,\n        invoice.id,\n      );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesOrderItem',\n          ci.orderItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n        const updatedOi = await tx.salesOrderItem.findUnique({\n     "
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "f createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesOrderItem',\n          ci.orderItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n        const updatedOi = await tx.salesOrderItem.findUnique({\n          where: { id: ci.orderItemId },\n        });\n        if (updatedOi)\n          await refreshLineStatus(\n            tx,\n            'salesOrderItem',\n            ci.orderItemId,\n            Number(updatedOi.openQty),\n          );\n      }\n\n      return invoice.id;\n    });\n    if (isIns === IS_CLIENT || isIns === IS_RESERVE)\n      return this._autoConfirmReserve(createdInvoiceId, tenantId, createdById);\n    return this.findOne(createdInvoiceId);\n  }\n\n  // =========================================================================\n  // CREATE FROM DELIVERY  — DO → SI(isIns=Y)\n  // ========================================"
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "ber;\n        deliveryOrderItemId: number;\n        qty: number;\n      }[] = [];\n      for (const ii of invoiceItems) {\n        const { _isInclusive: _inc2, ...lineData2 } = ii;\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...lineData2 },\n        });\n        createdItems.push({\n          id: created.id,\n          deliveryOrderItemId: ii.deliveryOrderItemId!,\n          qty: ii.quantity,\n        });\n      }\n\n      await linkDocuments(\n        tx,\n        tenantId,\n        DocumentType.DELIVERY_ORDER,\n        deliveryOrderId,\n        docType,\n        invoice.id,\n      );\n      if (delivery.orderId)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_ORDER,\n          delivery.orderId,\n          docType,\n          invoice.id,\n        );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'deliveryOrderItem',\n          ci.deliveryOrderItemId,\n          docType,\n          invoice.i"
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "OB',\n          exchangeRate,\n          createdById: createdById ?? null,\n        },\n      });\n\n      for (const ii of invoiceItems) {\n        const { _isInclusive: _inc3, ...lineData3 } = ii;\n        await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...lineData3 },\n        });\n      }\n\n      if (payload.salesQuotationId)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_QUOTATION,\n          payload.salesQuotationId,\n          docType,\n          invoice.id,\n        );\n      if (payload.salesOrderId)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_ORDER,\n          payload.salesOrderId,\n          docType,\n          invoice.id,\n        );\n\n      const createdInvoiceItems = await tx.saleInvoiceItem.findMany({\n        where: { saleInvoiceId: invoice.id },\n      });\n      for (const ci of createdInvoiceItems) {\n        if (!ci.baseLineId || !ci.baseDocType) continue;\n        const tableName: 'salesQuotationItem"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "nSourceLine(\n          tx,\n          tableName,\n          ci.baseLineId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        if (tableName === 'salesQuotationItem')\n          await tx.salesQuotationItem.update({\n            where: { id: ci.baseLineId },\n            data: {\n              orderedQty: { increment: Number(ci.quantity) },\n              openQty: { decrement: Number(ci.quantity) },\n            },\n          });\n        if (tableName === 'salesOrderItem')\n          await tx.salesOrderItem.update({\n            where: { id: ci.baseLineId },\n            data: { openQty: { decrement: Number(ci.quantity) } },\n          });\n      }\n\n      return invoice.id;\n    });\n    if (isIns === IS_CLIENT || isIns === IS_RESERVE)\n      return this._autoConfirmReserve(createdInvoiceId, tenantId, createdById);\n    return this.findOne(createdInvoiceId);\n  }\n\n  // =========================================================================\n  // CONFIRM\n  // isIns=N → mueve stock (stockPhysical ↓, stockCommitted ↓"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "          orderedQty: { increment: Number(ci.quantity) },\n              openQty: { decrement: Number(ci.quantity) },\n            },\n          });\n        if (tableName === 'salesOrderItem')\n          await tx.salesOrderItem.update({\n            where: { id: ci.baseLineId },\n            data: { openQty: { decrement: Number(ci.quantity) } },\n          });\n      }\n\n      return invoice.id;\n    });\n    if (isIns === IS_CLIENT || isIns === IS_RESERVE)\n      return this._autoConfirmReserve(createdInvoiceId, tenantId, createdById);\n    return this.findOne(createdInvoiceId);\n  }\n\n  // =========================================================================\n  // CONFIRM\n  // isIns=N → mueve stock (stockPhysical ↓, stockCommitted ↓ si venía de pedido)\n  // isIns=Y → no mueve stock (entrega ya lo hizo)\n  // =========================================================================\n  private async _executeConfirmLogic(\n    tx: Prisma.TransactionClient,\n    invoice: SaleInvoice,\n    tenantId: number,\n    confirmedById?: number,\n"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": ");\n\n      if (line.orderItemId) {\n        // FV directa (sin entrega previa): también incrementar deliveredQty\n        const isDirectSale = !line.deliveryOrderItemId && invoice.isIns === 'N';\n        await tx.salesOrderItem.update({\n          where: { id: line.orderItemId },\n          data: {\n            invoicedQty: { increment: Number(line.quantity) },\n            ...(isDirectSale\n              ? { deliveredQty: { increment: Number(line.quantity) } }\n              : {}),\n          },\n        });\n        const updatedOi = await tx.salesOrderItem.findUnique({\n          where: { id: line.orderItemId },\n        });\n        if (updatedOi)\n          await refreshLineStatus(\n            tx,\n            'salesOrderItem',\n            line.orderItemId,\n            Number(updatedOi.openQty),\n          );\n      }\n\n      if (line.deliveryOrderItemId) {\n        await tx.deliveryOrderItem.update({\n          where: { id: line.deliveryOrderItemId },\n          data: { invoicedQty: { increment: Number(line.quantity) } },\n        });"
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "freshLineStatus(\n            tx,\n            'salesOrderItem',\n            line.orderItemId,\n            Number(updatedOi.openQty),\n          );\n      }\n\n      if (line.deliveryOrderItemId) {\n        await tx.deliveryOrderItem.update({\n          where: { id: line.deliveryOrderItemId },\n          data: { invoicedQty: { increment: Number(line.quantity) } },\n        });\n        const updatedDi = await tx.deliveryOrderItem.findUnique({\n          where: { id: line.deliveryOrderItemId },\n        });\n        if (updatedDi) {\n          const pendingInDelivery =\n            Number(updatedDi.quantity) - Number(updatedDi.invoicedQty);\n          await refreshLineStatus(\n            tx,\n            'deliveryOrderItem',\n            line.deliveryOrderItemId,\n            pendingInDelivery,\n          );\n          // Propagar invoicedQty al SalesOrderItem si la entrega tiene línea de pedido\n          const soItemId = updatedDi.orderItemId;\n          if (soItemId) {\n            await tx.salesOrderItem.update({\n              where: { id: "
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "nDelivery,\n          );\n          // Propagar invoicedQty al SalesOrderItem si la entrega tiene línea de pedido\n          const soItemId = updatedDi.orderItemId;\n          if (soItemId) {\n            await tx.salesOrderItem.update({\n              where: { id: soItemId },\n              data: { invoicedQty: { increment: Number(line.quantity) } },\n            });\n            const updatedOi = await tx.salesOrderItem.findUnique({\n              where: { id: soItemId },\n            });\n            if (updatedOi)\n              await refreshLineStatus(\n                tx,\n                'salesOrderItem',\n                soItemId,\n                Number(updatedOi.openQty),\n              );\n          }\n        }\n      }\n    }\n\n    if (invoice.orderId) {\n      await refreshHeaderStatus(\n        tx,\n        'salesOrder',\n        'salesOrderItem',\n        invoice.orderId,\n        'OPEN',\n        'CLOSED',\n      );\n      await recalcSalesOrderProgress(tx, invoice.orderId);\n    }\n\n    // Recalcular pedido origen vía entrega (cuan"
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "items) {\n        if (line.deliveryOrderItemId) {\n          await clearTargetOnSourceLine(\n            tx,\n            'deliveryOrderItem',\n            line.deliveryOrderItemId,\n          );\n          await tx.deliveryOrderItem.update({\n            where: { id: line.deliveryOrderItemId },\n            data: { invoicedQty: { decrement: Number(line.quantity) } },\n          });\n          const di = await tx.deliveryOrderItem.findUnique({\n            where: { id: line.deliveryOrderItemId },\n          });\n          if (di) {\n            const pending = Number(di.quantity) - Number(di.invoicedQty ?? 0);\n            await refreshLineStatus(\n              tx,\n              'deliveryOrderItem',\n              line.deliveryOrderItemId,\n              pending,\n            );\n            // Revertir invoicedQty en SalesOrderItem si la entrega tiene línea de pedido\n            const soItemId = di.orderItemId;\n            if (soItemId) {\n              await tx.salesOrderItem.update({\n                where: { id: soItemId },\n            "
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "ending,\n            );\n            // Revertir invoicedQty en SalesOrderItem si la entrega tiene línea de pedido\n            const soItemId = di.orderItemId;\n            if (soItemId) {\n              await tx.salesOrderItem.update({\n                where: { id: soItemId },\n                data: { invoicedQty: { decrement: Number(line.quantity) } },\n              });\n              const updatedOi = await tx.salesOrderItem.findUnique({\n                where: { id: soItemId },\n              });\n              if (updatedOi)\n                await refreshLineStatus(\n                  tx,\n                  'salesOrderItem',\n                  soItemId,\n                  Number(updatedOi.openQty),\n                );\n            }\n          }\n        }\n\n        if (line.orderItemId) {\n          await clearTargetOnSourceLine(tx, 'salesOrderItem', line.orderItemId);\n          await tx.salesOrderItem.update({\n            where: { id: line.orderItemId },\n            data: { openQty: { increment: Number(line.quantity) } },\n       "
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "(updatedOi.openQty),\n                );\n            }\n          }\n        }\n\n        if (line.orderItemId) {\n          await clearTargetOnSourceLine(tx, 'salesOrderItem', line.orderItemId);\n          await tx.salesOrderItem.update({\n            where: { id: line.orderItemId },\n            data: { openQty: { increment: Number(line.quantity) } },\n          });\n          const oi = await tx.salesOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (oi)\n            await refreshLineStatus(\n              tx,\n              'salesOrderItem',\n              line.orderItemId,\n              Number(oi.openQty),\n            );\n        }\n\n        if (\n          line.baseDocType === DocumentType.SALES_QUOTATION &&\n          line.baseLineId\n        ) {\n          await tx.salesQuotationItem.update({\n            where: { id: line.baseLineId },\n            data: {\n              orderedQty: { decrement: Number(line.quantity) },\n              openQty: { increment: Number(line.quantity) },\n      "
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "ne.orderItemId,\n              Number(oi.openQty),\n            );\n        }\n\n        if (\n          line.baseDocType === DocumentType.SALES_QUOTATION &&\n          line.baseLineId\n        ) {\n          await tx.salesQuotationItem.update({\n            where: { id: line.baseLineId },\n            data: {\n              orderedQty: { decrement: Number(line.quantity) },\n              openQty: { increment: Number(line.quantity) },\n            },\n          });\n          const updatedQi = await tx.salesQuotationItem.findUnique({\n            where: { id: line.baseLineId },\n          });\n          if (updatedQi) {\n            await refreshLineStatus(\n              tx,\n              'salesQuotationItem',\n              line.baseLineId,\n              Number(updatedQi.openQty),\n            );\n            await tx.salesQuotation.updateMany({\n              where: { id: updatedQi.quotationId, status: 'CLOSED' },\n              data: { status: 'OPEN' },\n            });\n          }\n        }\n\n        if (invoice.isIns === IS_CLIENT) {\n       "
      },
      {
        "type": "update",
        "model": "saleInvoiceItem",
        "snippet": "? 0,\n            isInclusive,\n          });\n\n          const prevQty = Number(existing.quantity);\n          const diff = line.quantity - prevQty; // positivo si aumenta, negativo si reduce\n\n          await tx.saleInvoiceItem.update({\n            where: { id: line.id },\n            data: {\n              quantity: line.quantity,\n              subtotal: lc.lineNet,\n              openQty: line.quantity,\n              priceNet: lc.priceNet,\n              discountTotal: lc.discountTotal,\n              taxIndicatorId,\n              taxRate,\n              taxAmount: lc.taxAmount,\n              totalCost:\n                existing.cost != null\n                  ? Number(existing.cost) * line.quantity\n                  : null,\n              totalWeight:\n                existing.weight != null\n                  ? Number(existing.weight) * line.quantity\n                  : null,\n              batchId:\n                line.batchId !== undefined ? line.batchId : existing.batchId,\n              serialNumberId:\n                line.s"
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "       },\n          });\n\n          // Recalcular openQty en la fuente (DOItem o SOItem) si cambió la cantidad\n          if (diff !== 0) {\n            if (existing.deliveryOrderItemId) {\n              await tx.deliveryOrderItem.update({\n                where: { id: existing.deliveryOrderItemId },\n                data: { openQty: { decrement: diff } }, // si diff>0: openQty↓; si diff<0: openQty↑\n              });\n              const updatedDi = await tx.deliveryOrderItem.findUnique({\n                where: { id: existing.deliveryOrderItemId },\n              });\n              if (updatedDi)\n                await refreshLineStatus(\n                  tx,\n                  'deliveryOrderItem',\n                  existing.deliveryOrderItemId,\n                  Number(updatedDi.openQty),\n                );\n            } else if (existing.orderItemId) {\n              await tx.salesOrderItem.update({\n                where: { id: existing.orderItemId },\n                data: { openQty: { decrement: diff } },\n              });\n    "
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "      'deliveryOrderItem',\n                  existing.deliveryOrderItemId,\n                  Number(updatedDi.openQty),\n                );\n            } else if (existing.orderItemId) {\n              await tx.salesOrderItem.update({\n                where: { id: existing.orderItemId },\n                data: { openQty: { decrement: diff } },\n              });\n              const updatedOi = await tx.salesOrderItem.findUnique({\n                where: { id: existing.orderItemId },\n              });\n              if (updatedOi)\n                await refreshLineStatus(\n                  tx,\n                  'salesOrderItem',\n                  existing.orderItemId,\n                  Number(updatedOi.openQty),\n                );\n            }\n          }\n        }\n\n        const allItems = await tx.saleInvoiceItem.findMany({\n          where: { saleInvoiceId: invoiceId },\n          include: { taxIndicator: true },\n        });\n        const lineResults = allItems.map((i) => ({\n          lineNet: Number(i.subtotal),\n         "
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "emId: number;\n        deliveryId: number;\n        qty: number;\n      }[] = [];\n      for (const ii of invoiceItems) {\n        const { _isInclusive: _inc4, ...lineData4 } = ii;\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...lineData4 },\n        });\n        const linePayload = payload.items.find(\n          (l) => l.deliveryOrderItemId === ii.deliveryOrderItemId,\n        );\n        createdItems.push({\n          id: created.id,\n          deliveryOrderItemId: ii.deliveryOrderItemId!,\n          deliveryId: linePayload!.deliveryId,\n          qty: ii.quantity,\n        });\n      }\n\n      // Trazabilidad: DocumentLink por cada entrega + targetEntry en líneas de entrega\n      for (const deliveryId of deliveryIds) {\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.DELIVERY_ORDER,\n          deliveryId,\n          docType,\n          invoice.id,\n        );\n      }\n\n      // Linkar también al pedido de venta origen si existe\n      const or"
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "r;\n        quotationItemId: number;\n        qty: number;\n      }[] = [];\n      for (const ii of invoiceItems) {\n        const { _quotationItemId, _isInclusive, ...data } = ii;\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...data },\n        });\n        createdItems.push({\n          id: created.id,\n          quotationItemId: _quotationItemId!,\n          qty: ii.quantity,\n        });\n      }\n\n      for (const quotationId of quotationIds)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_QUOTATION,\n          quotationId,\n          docType,\n          invoice.id,\n        );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          ci.quotationItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { i"
      },
      {
        "type": "update",
        "model": "salesQuotationItem",
        "snippet": "dItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesQuotationItem',\n          ci.quotationItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n        const updatedQi = await tx.salesQuotationItem.findUnique({\n          where: { id: ci.quotationItemId },\n        });\n        if (updatedQi)\n          await refreshLineStatus(\n            tx,\n            'salesQuotationItem',\n            ci.quotationItemId,\n            Number(updatedQi.openQty),\n          );\n      }\n\n      for (const quotationId of quotationIds) {\n        const allQItems = await tx.salesQuotationItem.findMany({\n          where: { quotationId },\n        });\n        if (allQItems.every((i) => Number(i.openQty) <= 0))\n          await tx.salesQuotation.update({\n            where: { id: quotati"
      },
      {
        "type": "create",
        "model": "saleInvoiceItem",
        "snippet": "      const createdItems: { id: number; orderItemId: number; qty: number }[] =\n        [];\n      for (const ii of invoiceItems) {\n        const { _isInclusive, ...data } = ii;\n        const created = await tx.saleInvoiceItem.create({\n          data: { saleInvoiceId: invoice.id, ...data },\n        });\n        createdItems.push({\n          id: created.id,\n          orderItemId: ii.orderItemId!,\n          qty: ii.quantity,\n        });\n      }\n\n      for (const orderId of orderIds)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.SALES_ORDER,\n          orderId,\n          docType,\n          invoice.id,\n        );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesOrderItem',\n          ci.orderItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n        const updat"
      },
      {
        "type": "update",
        "model": "salesOrderItem",
        "snippet": "f createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'salesOrderItem',\n          ci.orderItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.salesOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n        const updatedOi = await tx.salesOrderItem.findUnique({\n          where: { id: ci.orderItemId },\n        });\n        if (updatedOi)\n          await refreshLineStatus(\n            tx,\n            'salesOrderItem',\n            ci.orderItemId,\n            Number(updatedOi.openQty),\n          );\n      }\n\n      return invoice.id;\n    });\n    if (isIns === IS_CLIENT || isIns === IS_RESERVE)\n      return this._autoConfirmReserve(createdInvoiceId, tenantId, createdById);\n    return this.findOne(createdInvoiceId);\n  }\n\n  async _resolvePricesFromList(prismaClient, partnerId, items) {\n    try {\n      const partner = await prismaClient.partner.findUnique({\n        where: { id: partnerId },"
      }
    ]
  },
  {
    "file": "purchase-quotations/purchase-quotations.service.ts",
    "matches": [
      {
        "type": "createMany",
        "model": "purchaseQuotationItem",
        "snippet": "    totalDiscountInBaseCurrency: baseTotals.totalDiscount,\n          currency: dto.currency ?? 'BOB',\n          exchangeRate: dto.exchangeRate ?? 1,\n          createdById,\n        },\n      });\n\n      await tx.purchaseQuotationItem.createMany({\n        data: lineResults.map((l, index) => ({\n          tenantId,\n          quotationId: quotation.id,\n          itemId: l.itemId,\n          itemCode: l.itemCode,\n          itemName: l.itemName,\n          lineNum: index,\n          quantity: l.quantity,\n          price: l.price,\n          discountPct: l.discountPct,\n          discountAmt: l.discountAmt,\n          discountTotal: l.discountTotal,\n          priceNet: l.priceNet,\n          subtotal: l.subtotal,\n          openQty: l.quantity,\n          orderedQty: 0,\n          cost: l.cost,\n          totalCost: l.totalCost,\n          weight: l.weight,\n          totalWeight: l.totalWeight,\n          warehouseId: l.warehouseId ?? null,\n          taxIndicatorId: l.taxIndicatorId ?? null,\n          taxRate: l.taxRate,\n          taxAmount: l.taxAm"
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "d no puede ser menor a lo ya pedido (${existing.orderedQty})`,\n            );\n          }\n          const newPQOpenQty =\n            Number(incoming.quantity) - Number(existing.orderedQty);\n          await tx.purchaseQuotationItem.update({\n            where: { id: incoming.id },\n            data: {\n              itemId: incoming.itemId,\n              itemCode: catalogItem.code,\n              itemName: catalogItem.name,\n              quantity: incoming.quantity,\n              openQty: newPQOpenQty,\n              price: incoming.price,\n              discountPct:\n                discountMode === 'line' ? (incoming.discountPct ?? null) : null,\n              discountAmt:\n                discountMode === 'line' ? (incoming.discountAmt ?? null) : null,\n              discountTotal: lineCalc.discountTotal,\n              priceNet: lineCalc.priceNet,\n              subtotal: lineCalc.lineFinal,\n              cost,\n              totalCost,\n              weight,\n              totalWeight,\n              warehouseId: lineWh ?? null,\n     "
      },
      {
        "type": "create",
        "model": "purchaseQuotationItem",
        "snippet": "t,\n              warehouseId: lineWh ?? null,\n              taxIndicatorId,\n              taxRate,\n              taxAmount: lineCalc.taxAmount,\n            },\n          });\n        } else {\n          await tx.purchaseQuotationItem.create({\n            data: {\n              tenantId,\n              quotationId: id,\n              itemId: incoming.itemId,\n              itemCode: catalogItem.code,\n              itemName: catalogItem.name,\n              lineNum:\n                currentLines.length +\n                lineResults.findIndex((lr) => lr.incoming === incoming),\n              quantity: incoming.quantity,\n              price: incoming.price,\n              discountPct:\n                discountMode === 'line' ? (incoming.discountPct ?? null) : null,\n              discountAmt:\n                discountMode === 'line' ? (incoming.discountAmt ?? null) : null,\n              discountTotal: lineCalc.discountTotal,\n              priceNet: lineCalc.priceNet,\n              subtotal: lineCalc.lineFinal,\n              openQty: incomin"
      }
    ]
  },
  {
    "file": "purchase-orders/purchase-orders.service.ts",
    "matches": [
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "ields(\n            DocumentType.PURCHASE_QUOTATION,\n            dto.quotationId,\n            qItem.id,\n            qItem.lineNum ?? index,\n            line.quantity,\n          ),\n        });\n\n        await tx.purchaseQuotationItem.update({\n          where: { id: qItem.id },\n          data: {\n            orderedQty: { increment: line.quantity },\n            openQty: { decrement: line.quantity },\n          },\n        });\n        itemLeadTimes.push(qItem.item.leadTime ?? null);\n      }\n\n      const orderDate = dto.date ? new Date(dto.date) : new Date();\n      const expectedDate = dto.expectedDate\n        ? new Date(dto.expectedDate)\n        : this.calcExpectedDate(orderDate, itemLeadTimes);\n\n      let docTotals: {\n        total: number;\n        subtotal: number;\n        tax: number;\n        totalDiscount: number;\n      };\n      if (discountMode === 'header') {\n        const dt = calcDocumentTotalsWithIndicators(\n          lineCalcs.map((l) => ({\n            lineNet: l.lineNet,\n            taxRate: l.taxRate ?? 0,\n            "
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "DocumentType.PURCHASE_QUOTATION,\n        dto.quotationId,\n        DocumentType.PURCHASE_ORDER,\n        order.id,\n      );\n\n      for (const oi of order.items) {\n        if (oi.baseLineId) {\n          await tx.purchaseQuotationItem.update({\n            where: { id: oi.baseLineId },\n            data: {\n              targetDocType: DocumentType.PURCHASE_ORDER,\n              targetDocId: order.id,\n              targetLineId: oi.id,\n            },\n          });\n          const qItem = quotation.items.find((i) => i.id === oi.baseLineId)!;\n          const pending =\n            Number(qItem.quantity) -\n            (Number(qItem.orderedQty) + Number(oi.quantity));\n          await refreshLineStatus(\n            tx,\n            'purchaseQuotationItem',\n            qItem.id,\n            pending,\n          );\n        }\n      }\n\n      const whItemMap = new Map<number, Set<number>>();\n      for (const oi of orderItems) {\n        const wId = oi.warehouseId ?? warehouseId;\n        if (wId) {\n          if (!whItemMap.has(wId)) whItemMap.set"
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "elds(\n            DocumentType.PURCHASE_QUOTATION,\n            line.quotationId,\n            qItem.id,\n            qItem.lineNum ?? index,\n            line.quantity,\n          ),\n        });\n\n        await tx.purchaseQuotationItem.update({\n          where: { id: qItem.id },\n          data: {\n            orderedQty: { increment: line.quantity },\n            openQty: { decrement: line.quantity },\n          },\n        });\n        itemLeadTimes.push(qItem.item.leadTime ?? null);\n      }\n\n      const orderDate = payload.date ? new Date(payload.date) : new Date();\n      const expectedDate = payload.expectedDate\n        ? new Date(payload.expectedDate)\n        : this.calcExpectedDate(orderDate, itemLeadTimes);\n\n      const docTotals = calcDocumentTotalsFromLines(lineCalcs);\n      const baseTotals = calcBaseCurrencyTotals(\n        docTotals,\n        Number(payload.exchangeRate ?? quotations[0].exchangeRate ?? 1),\n      );\n\n      const order = await tx.purchaseOrder.create({\n        data: {\n          tenantId,\n          customField"
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": ".PURCHASE_QUOTATION,\n          quotationId,\n          DocumentType.PURCHASE_ORDER,\n          order.id,\n        );\n      }\n\n      for (const oi of order.items) {\n        if (oi.baseLineId) {\n          await tx.purchaseQuotationItem.update({\n            where: { id: oi.baseLineId },\n            data: {\n              targetDocType: DocumentType.PURCHASE_ORDER,\n              targetDocId: order.id,\n              targetLineId: oi.id,\n            },\n          });\n          const quotation = quotations.find((q) => q.id === oi.baseDocId);\n          const qItem = quotation?.items.find((i) => i.id === oi.baseLineId);\n          if (qItem) {\n            const updatedQItem = await tx.purchaseQuotationItem.findUnique({\n              where: { id: qItem.id },\n            });\n            if (updatedQItem)\n              await refreshLineStatus(\n                tx,\n                'purchaseQuotationItem',\n                qItem.id,\n                Number(updatedQItem.openQty),\n              );\n          }\n        }\n      }\n\n      const whItemM"
      },
      {
        "type": "create",
        "model": "purchaseOrderItem",
        "snippet": "urrency ?? 'BOB',\n          exchangeRate: payload.exchangeRate ?? 1,\n          createdById: createdById ?? null,\n        },\n      });\n\n      for (const [index, lr] of lineResults.entries()) {\n        await tx.purchaseOrderItem.create({\n          data: {\n            tenantId,\n            orderId: order.id,\n            itemId: lr.itemId,\n            itemName: lr.itemName,\n            itemCode: lr.itemCode,\n            quantity: lr.quantity,\n            price: lr.price,\n            discountPct: lr.discountPct,\n            discountAmt: lr.discountAmt,\n            discountTotal: lr.discountTotal,\n            priceNet: lr.priceNet,\n            subtotal: lr.subtotal,\n            cost: lr.cost,\n            totalCost: lr.totalCost,\n            weight: lr.weight,\n            totalWeight: lr.totalWeight,\n            warehouseId: lr.warehouseId,\n            taxIndicatorId: lr.taxIndicatorId ?? null,\n            taxRate: lr.taxRate,\n            taxAmount: lr.taxAmount,\n            lineNum: index,\n            openQty: lr.quantity,\n "
      },
      {
        "type": "create",
        "model": "purchaseOrderItem",
        "snippet": "     const lineWh = quotationItem.warehouseId ?? order.warehouseId ?? null;\n      const existingCount = await tx.purchaseOrderItem.count({\n        where: { orderId },\n      });\n\n      const newItem = await tx.purchaseOrderItem.create({\n        data: {\n          tenantId: tenantId as number,\n          orderId,\n          quotationItemId: quotationItem.id,\n          itemId: quotationItem.itemId,\n          itemName: quotationItem.item.name,\n          itemCode: quotationItem.item.code,\n          quantity: dto.quantity,\n          price,\n          discountPct,\n          discountAmt,\n          discountTotal: lineCalc.discountTotal,\n          priceNet: lineCalc.priceNet,\n          subtotal: lineCalc.lineFinal,\n          warehouseId: lineWh,\n          taxIndicatorId: taxIndicatorId ?? null,\n          taxRate,\n          taxAmount: lineCalc.taxAmount,\n          lineNum: existingCount,\n          projectCode: dto.projectCode ?? null,\n          dimension1: dto.dimension1 ?? null,\n          dimension2: dto.dimension2 ?? null,\n        "
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "QUOTATION,\n            order.purchaseQuotationId!,\n            quotationItem.id,\n            quotationItem.lineNum ?? existingCount,\n            dto.quantity,\n          ),\n        },\n      });\n\n      await tx.purchaseQuotationItem.update({\n        where: { id: quotationItem.id },\n        data: {\n          targetDocType: DocumentType.PURCHASE_ORDER,\n          targetDocId: orderId,\n          targetLineId: newItem.id,\n        },\n      });\n      await tx.purchaseQuotationItem.update({\n        where: { id: quotationItem.id },\n        data: {\n          orderedQty: { increment: dto.quantity },\n          openQty: { decrement: dto.quantity },\n        },\n      });\n\n      const updatedQItem = await tx.purchaseQuotationItem.findUnique({\n        where: { id: quotationItem.id },\n      });\n      if (updatedQItem) {\n        await refreshLineStatus(\n          tx,\n          'purchaseQuotationItem',\n          quotationItem.id,\n          Number(updatedQItem.openQty),\n        );\n      }\n\n      await tx.purchaseOrder.update({\n        where: { i"
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "  where: { id: quotationItem.id },\n        data: {\n          targetDocType: DocumentType.PURCHASE_ORDER,\n          targetDocId: orderId,\n          targetLineId: newItem.id,\n        },\n      });\n      await tx.purchaseQuotationItem.update({\n        where: { id: quotationItem.id },\n        data: {\n          orderedQty: { increment: dto.quantity },\n          openQty: { decrement: dto.quantity },\n        },\n      });\n\n      const updatedQItem = await tx.purchaseQuotationItem.findUnique({\n        where: { id: quotationItem.id },\n      });\n      if (updatedQItem) {\n        await refreshLineStatus(\n          tx,\n          'purchaseQuotationItem',\n          quotationItem.id,\n          Number(updatedQItem.openQty),\n        );\n      }\n\n      await tx.purchaseOrder.update({\n        where: { id: orderId },\n        data: { updatedById: updatedById ?? null },\n      });\n      await this.recalculateTotals(orderId, tx);\n      if (lineWh)\n        await recalcOrdered(tx, tenantId, quotationItem.itemId, lineWh);\n      await this.recalculateQu"
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "n('Cantidad excede lo cotizado');\n          const diff = dto.quantity - Number(line.quantity);\n          // diff>0: más qty → orderedQty↑ openQty↓ | diff<0: menos qty → orderedQty↓ openQty↑\n          await tx.purchaseQuotationItem.update({\n            where: { id: quotationItem.id },\n            data: {\n              orderedQty: { increment: diff },\n              openQty: { decrement: diff },\n            },\n          });\n          const updatedQItem = await tx.purchaseQuotationItem.findUnique({\n            where: { id: quotationItem.id },\n          });\n          if (updatedQItem)\n            await refreshLineStatus(\n              tx,\n              'purchaseQuotationItem',\n              quotationItem.id,\n              Number(updatedQItem.openQty),\n            );\n        }\n      }\n\n      const supplier = await tx.partner.findUnique({\n        where: { id: order.supplierId },\n      });\n      const defaultIndicator = await loadDefaultIndicator(tx);\n      const supplierTaxInfo = {\n        taxRequired: supplier?.taxRequired ?? fa"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "?? null,\n        },\n        lineTaxId,\n        price,\n        dto.quantity,\n        invoiced,\n        discountPct,\n        discountAmt,\n        discountMode,\n        defaultIndicator,\n      );\n\n      await tx.purchaseOrderItem.update({\n        where: { id: lineId },\n        data: {\n          quantity: dto.quantity,\n          discountPct,\n          discountAmt,\n          discountTotal: lineCalc.discountTotal,\n          priceNet: lineCalc.priceNet,\n          subtotal: lineCalc.lineFinal,\n          baseQty: dto.quantity,\n          taxIndicatorId: taxIndicatorId ?? null,\n          taxRate,\n          taxAmount: lineCalc.taxAmount,\n          projectCode: dto.projectCode ?? null,\n          dimension1: dto.dimension1 ?? null,\n          dimension2: dto.dimension2 ?? null,\n        },\n      });\n\n      await tx.purchaseOrder.update({\n        where: { id: orderId },\n        data: { updatedById: updatedById ?? null },\n      });\n      await this.recalculateTotals(orderId, tx);\n\n      const lineWh = line.warehouseId ?? order.warehouse"
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "e.receivedQty) > 0)\n        throw new BadRequestException(\n          'No se puede quitar una línea que ya tiene recepciones registradas',\n        );\n\n      if (line.quotationItemId != null) {\n        await tx.purchaseQuotationItem.update({\n          where: { id: line.quotationItemId },\n          data: {\n            orderedQty: { decrement: Number(line.quantity) },\n            openQty: { increment: Number(line.quantity) },\n          },\n        });\n        await clearTargetOnSourceLine(\n          tx,\n          'purchaseQuotationItem',\n          line.quotationItemId,\n        );\n        const qItem = await tx.purchaseQuotationItem.findUnique({\n          where: { id: line.quotationItemId },\n        });\n        if (qItem)\n          await refreshLineStatus(\n            tx,\n            'purchaseQuotationItem',\n            qItem.id,\n            Number(qItem.openQty),\n          );\n      }\n\n      await tx.purchaseOrderItem.delete({ where: { id: lineId } });\n      await tx.purchaseOrder.update({\n        where: { id: orderId },\n       "
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "d = false;\n            break;\n          }\n        }\n        wasAutoClosedFull = allFullyCovered;\n      }\n\n      for (const line of order.items) {\n        if (line.quotationItemId != null) {\n          await tx.purchaseQuotationItem.update({\n            where: { id: line.quotationItemId },\n            data: {\n              orderedQty: { decrement: Number(line.quantity) },\n              openQty: { increment: Number(line.quantity) },\n            },\n          });\n          await clearTargetOnSourceLine(\n            tx,\n            'purchaseQuotationItem',\n            line.quotationItemId,\n          );\n          const qItem = await tx.purchaseQuotationItem.findUnique({\n            where: { id: line.quotationItemId },\n          });\n          if (qItem)\n            await refreshLineStatus(\n              tx,\n              'purchaseQuotationItem',\n              qItem.id,\n              Number(qItem.openQty),\n            );\n        }\n        await tx.purchaseOrderItem.update({\n          where: { id: line.id },\n          data: { quotat"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": " if (qItem)\n            await refreshLineStatus(\n              tx,\n              'purchaseQuotationItem',\n              qItem.id,\n              Number(qItem.openQty),\n            );\n        }\n        await tx.purchaseOrderItem.update({\n          where: { id: line.id },\n          data: { quotationItemId: null },\n        });\n      }\n\n      await tx.purchaseOrder.update({\n        where: { id },\n        data: {\n          status: PurchaseOrderStatus.CANCELLED,\n          updatedById: updatedById ?? null,\n        },\n      });\n      await tx.purchaseOrderItem.updateMany({\n        where: { orderId: id },\n        data: { lineStatus: 'CANCELLED' },\n      });\n\n      for (const qId of quotationIds) {\n        await removeDocumentLink(\n          tx,\n          tenantId,\n          DocumentType.PURCHASE_QUOTATION,\n          qId,\n          DocumentType.PURCHASE_ORDER,\n          id,\n        );\n      }\n\n      if (order.warehouseId) {\n        const itemIds = [...new Set(order.items.map((i) => i.itemId))];\n        await Promise.all(\n        "
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "t linkedLines = await tx.purchaseOrderItem.findMany({\n          where: { orderId: id },\n        });\n        for (const line of linkedLines) {\n          if (line.quotationItemId != null) {\n            await tx.purchaseQuotationItem.update({\n              where: { id: line.quotationItemId },\n              data: {\n                orderedQty: { decrement: Number(line.quantity) },\n                openQty: { increment: Number(line.quantity) },\n              },\n            });\n            await clearTargetOnSourceLine(\n              tx,\n              'purchaseQuotationItem',\n              line.quotationItemId,\n            );\n            const qItem = await tx.purchaseQuotationItem.findUnique({\n              where: { id: line.quotationItemId },\n            });\n            if (qItem)\n              await refreshLineStatus(\n                tx,\n                'purchaseQuotationItem',\n                qItem.id,\n                Number(qItem.openQty),\n              );\n          }\n        }\n        await tx.purchaseOrderItem.updateMany({\n"
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "ffectedItemIds = new Set<number>();\n\n      for (const line of currentLines) {\n        if (!(dto.items ?? []).find((i) => i.id === line.id)) {\n          if (line.quotationItemId != null) {\n            await tx.purchaseQuotationItem.update({\n              where: { id: line.quotationItemId },\n              data: {\n                orderedQty: { decrement: Number(line.quantity) },\n                openQty: { increment: Number(line.quantity) },\n              },\n            });\n            await clearTargetOnSourceLine(\n              tx,\n              'purchaseQuotationItem',\n              line.quotationItemId,\n            );\n            const qItem = await tx.purchaseQuotationItem.findUnique({\n              where: { id: line.quotationItemId },\n            });\n            if (qItem)\n              await refreshLineStatus(\n                tx,\n                'purchaseQuotationItem',\n                qItem.id,\n                Number(qItem.openQty),\n              );\n          }\n          await tx.purchaseOrderItem.delete({ where: { id:"
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "     invoiced,\n            discountPct,\n            discountAmt,\n            discountMode,\n            defaultIndicator,\n          );\n        lineCalcs.push(lineCalc);\n\n        if (qItem) {\n          await tx.purchaseQuotationItem.update({\n            where: { id: qItem.id },\n            data: {\n              orderedQty: { increment: diff },\n              openQty: { decrement: diff },\n            },\n          });\n          affectedItemIds.add(qItem.itemId);\n          const updatedQItem = await tx.purchaseQuotationItem.findUnique({\n            where: { id: qItem.id },\n          });\n          if (updatedQItem)\n            await refreshLineStatus(\n              tx,\n              'purchaseQuotationItem',\n              qItem.id,\n              Number(updatedQItem.openQty),\n            );\n        }\n\n        if (i.id) {\n          await tx.purchaseOrderItem.update({\n            where: { id: i.id },\n            data: {\n              quantity: i.quantity,\n              discountPct,\n              discountAmt,\n              discountTot"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "refreshLineStatus(\n              tx,\n              'purchaseQuotationItem',\n              qItem.id,\n              Number(updatedQItem.openQty),\n            );\n        }\n\n        if (i.id) {\n          await tx.purchaseOrderItem.update({\n            where: { id: i.id },\n            data: {\n              quantity: i.quantity,\n              discountPct,\n              discountAmt,\n              discountTotal: lineCalc.discountTotal,\n              priceNet: lineCalc.priceNet,\n              subtotal: lineCalc.lineFinal,\n              baseQty: i.quantity,\n              taxIndicatorId: taxIndicatorId ?? null,\n              taxRate,\n              taxAmount: lineCalc.taxAmount,\n              projectCode: i.projectCode ?? null,\n              dimension1: i.dimension1 ?? null,\n              dimension2: i.dimension2 ?? null,\n            },\n          });\n        } else {\n          const baseFieldsData =\n            currentOrder.purchaseQuotationId && qItem\n              ? buildBaseFields(\n                  DocumentType.PURCHASE_QUOTAT"
      },
      {
        "type": "create",
        "model": "purchaseOrderItem",
        "snippet": "  projectCode: i.projectCode ?? null,\n            dimension1: i.dimension1 ?? null,\n            dimension2: i.dimension2 ?? null,\n            ...baseFieldsData,\n          };\n          const newItem = await tx.purchaseOrderItem.create({\n            data: itemData,\n          });\n          if (qItem) {\n            await tx.purchaseQuotationItem.update({\n              where: { id: qItem.id },\n              data: {\n                targetDocType: DocumentType.PURCHASE_ORDER,\n                targetDocId: id,\n                targetLineId: newItem.id,\n              },\n            });\n          }\n        }\n      }\n\n      let docTotals: {\n        total: number;\n        subtotal: number;\n        tax: number;\n        totalDiscount: number;\n      };\n      if (discountMode === 'header') {\n        const dt = calcDocumentTotalsWithIndicators(\n          lineCalcs.map((l) => ({\n            lineNet: l.lineNet,\n            taxRate: l.taxRate ?? 0,\n            taxAmount: l.taxAmount,\n            lineFinal: l.lineFinal,\n          })),\n      "
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "ension2 ?? null,\n            ...baseFieldsData,\n          };\n          const newItem = await tx.purchaseOrderItem.create({\n            data: itemData,\n          });\n          if (qItem) {\n            await tx.purchaseQuotationItem.update({\n              where: { id: qItem.id },\n              data: {\n                targetDocType: DocumentType.PURCHASE_ORDER,\n                targetDocId: id,\n                targetLineId: newItem.id,\n              },\n            });\n          }\n        }\n      }\n\n      let docTotals: {\n        total: number;\n        subtotal: number;\n        tax: number;\n        totalDiscount: number;\n      };\n      if (discountMode === 'header') {\n        const dt = calcDocumentTotalsWithIndicators(\n          lineCalcs.map((l) => ({\n            lineNet: l.lineNet,\n            taxRate: l.taxRate ?? 0,\n            taxAmount: l.taxAmount,\n            lineFinal: l.lineFinal,\n          })),\n          invoiced,\n          dto.headerDiscountPct,\n          dto.headerDiscountAmt,\n        );\n        docTotals = {\n    "
      }
    ]
  },
  {
    "file": "purchase-receipts/purchase-receipts.service.ts",
    "matches": [
      {
        "type": "create",
        "model": "purchaseReceiptItem",
        "snippet": "Math.round((lineFinal - lineTaxAmount) * 100) / 100;\n\n        headerSubtotal += lineNet;\n        headerTax += lineTaxAmount;\n        headerTotalDiscount += lineDiscountTotal;\n\n        const created = await tx.purchaseReceiptItem.create({\n          data: {\n            tenantId: tenantId as number,\n            purchaseReceiptId: receipt.id,\n            orderItemId: line.orderItemId,\n            itemId: oi.itemId,\n            itemCode: oi.itemCode ?? oi.item.code,\n            itemName: oi.itemName ?? oi.item.name,\n            quantity: line.quantity,\n            price: linePrice,\n            priceNet: linePriceNet,\n            discountPct: lineDiscountPct,\n            discountAmt: lineDiscountAmt,\n            discountTotal: lineDiscountTotal,\n            subtotal: lineNet,\n            lineNet: lineNet,\n            lineFinal: lineFinal,\n            taxIndicatorId: oi.taxIndicatorId ?? null,\n            taxRate: lineTaxRate,\n            taxAmount: lineTaxAmount,\n            cost,\n            totalCost: cost != null ? cost * l"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "\n          DocumentType.PURCHASE_RECEIPT,\n          receipt.id,\n          ci.id,\n        );\n\n        // Decrementar openQty en la línea de la orden (ahora está reservado en la recepción OPEN)\n        await tx.purchaseOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n        const updatedOI = await tx.purchaseOrderItem.findUnique({\n          where: { id: ci.orderItemId },\n        });\n        await refreshLineStatus(\n          tx,\n          'purchaseOrderItem',\n          ci.orderItemId,\n          Number(updatedOI?.openQty ?? 0),\n        );\n      }\n\n      // ── Actualizar totales de cabecera ─────────────────────────────\n      const allItems = await tx.purchaseReceiptItem.findMany({\n        where: { purchaseReceiptId: receipt.id },\n      });\n      const hdrTotalCost = allItems.reduce(\n        (s, i) => s + Number(i.totalCost ?? 0),\n        0,\n      );\n      const hdrTotalWeight = allItems.some((i) => i.totalWeight != null)\n        ? allItems.reduce("
      },
      {
        "type": "createMany",
        "model": "purchaseReceiptItem",
        "snippet": "urrency: baseTotals.total,\n          totalDiscountInBaseCurrency: baseTotals.totalDiscount,\n          totalCost: headerTotalCost,\n          totalWeight: headerTotalWeight,\n        },\n      });\n\n      await tx.purchaseReceiptItem.createMany({\n        data: receiptItems.map((ri) => ({\n          purchaseReceiptId: receipt.id,\n          ...ri,\n        })),\n      });\n\n      // Recalcular stockOrdered para los artículos afectados\n      const whItemMap = new Map<number, Set<number>>();\n      for (const ri of receiptItems) {\n        if (ri.warehouseId) {\n          if (!whItemMap.has(ri.warehouseId))\n            whItemMap.set(ri.warehouseId, new Set());\n          whItemMap.get(ri.warehouseId)!.add(ri.itemId);\n        }\n      }\n      for (const [wId, ids] of whItemMap) {\n        for (const itemId of ids)\n          await recalcOrdered(tx, tenantId, itemId, wId);\n      }\n\n      // Trazabilidad: vincular con documento origen si viene de \"Copiar a\"\n      if (payload.purchaseQuotationId) {\n        await linkDocuments(\n          tx,\n       "
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "      ci.id,\n            );\n          }\n          // ── Consumir openQty en la cotización origen ──────────────────────\n          if (ci.baseDocType === DocumentType.PURCHASE_QUOTATION) {\n            await tx.purchaseQuotationItem.update({\n              where: { id: ci.baseLineId },\n              data: {\n                orderedQty: { increment: ci.quantity },\n                openQty: { decrement: ci.quantity },\n              },\n            });\n            const updatedQI = await tx.purchaseQuotationItem.findUnique({\n              where: { id: ci.baseLineId },\n            });\n            if (updatedQI)\n              await refreshLineStatus(\n                tx,\n                'purchaseQuotationItem',\n                ci.baseLineId,\n                Number(updatedQI.openQty),\n              );\n          }\n        }\n      }\n\n      return receipt.id;\n    });\n\n    // Auto-confirmar la recepción manual: suma stockPhysical inmediatamente\n    return this.confirm(receiptId, tenantId, createdById);\n  }\n\n  // ==========================="
      },
      {
        "type": "update",
        "model": "purchaseReceiptItem",
        "snippet": "     });\n\n      // ── receivedQty + openQty + lineStatus en la orden (solo si no es manual) ──\n      for (const line of items) {\n        // Marcar línea de recepción como CLOSED (openQty = 0)\n        await tx.purchaseReceiptItem.update({\n          where: { id: line.id },\n          data: { openQty: 0 },\n        });\n        await refreshLineStatus(tx, 'purchaseReceiptItem', line.id, 0);\n\n        if (!isManual && line.orderItemId) {\n          // receivedQty ↑, openQty ya fue decrementado al crear la recepción\n          await tx.purchaseOrderItem.update({\n            where: { id: line.orderItemId },\n            data: { receivedQty: { increment: Number(line.quantity) } },\n          });\n\n          const updatedOI = await tx.purchaseOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (updatedOI) {\n            await refreshLineStatus(\n              tx,\n              'purchaseOrderItem',\n              line.orderItemId,\n              Number(updatedOI.openQty),\n            );\n          }\n "
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "  await refreshLineStatus(tx, 'purchaseReceiptItem', line.id, 0);\n\n        if (!isManual && line.orderItemId) {\n          // receivedQty ↑, openQty ya fue decrementado al crear la recepción\n          await tx.purchaseOrderItem.update({\n            where: { id: line.orderItemId },\n            data: { receivedQty: { increment: Number(line.quantity) } },\n          });\n\n          const updatedOI = await tx.purchaseOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (updatedOI) {\n            await refreshLineStatus(\n              tx,\n              'purchaseOrderItem',\n              line.orderItemId,\n              Number(updatedOI.openQty),\n            );\n          }\n        }\n      }\n\n      // ── Movimientos de stock + upsertStock ──────────────────────\n      const whItemMap = new Map<number, Set<number>>();\n\n      for (const line of items) {\n        const lineWarehouseId = line.warehouseId ?? headerWarehouseId;\n        if (!lineWarehouseId)\n          throw new BadRequestException("
      },
      {
        "type": "update",
        "model": "purchaseReceiptItem",
        "snippet": "     }\n        }\n      }\n\n      // ── Limpiar trazabilidad y restaurar openQty en líneas de la orden ──\n      for (const line of items) {\n        // Restaurar openQty en la línea de recepción\n        await tx.purchaseReceiptItem.update({\n          where: { id: line.id },\n          data: { openQty: Number(line.quantity) },\n        });\n\n        if (!isManual && line.orderItemId) {\n          // Restaurar openQty en la línea de la orden\n          await tx.purchaseOrderItem.update({\n            where: { id: line.orderItemId },\n            data: { openQty: { increment: Number(line.quantity) } },\n          });\n\n          await clearTargetOnSourceLine(\n            tx,\n            'purchaseOrderItem',\n            line.orderItemId,\n          );\n\n          const oi = await tx.purchaseOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (oi) {\n            await refreshLineStatus(\n              tx,\n              'purchaseOrderItem',\n              line.orderItemId,\n              Number(oi.open"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": " where: { id: line.id },\n          data: { openQty: Number(line.quantity) },\n        });\n\n        if (!isManual && line.orderItemId) {\n          // Restaurar openQty en la línea de la orden\n          await tx.purchaseOrderItem.update({\n            where: { id: line.orderItemId },\n            data: { openQty: { increment: Number(line.quantity) } },\n          });\n\n          await clearTargetOnSourceLine(\n            tx,\n            'purchaseOrderItem',\n            line.orderItemId,\n          );\n\n          const oi = await tx.purchaseOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (oi) {\n            await refreshLineStatus(\n              tx,\n              'purchaseOrderItem',\n              line.orderItemId,\n              Number(oi.openQty),\n            );\n          }\n        }\n      }\n\n      if (!isManual && receipt.purchaseOrderId) {\n        // Eliminar link de documento\n        await removeDocumentLink(\n          tx,\n          receipt.tenantId,\n          DocumentType.PURCHA"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "         tenantId,\n                serialNumberId,\n                'SOLD',\n                null,\n              );\n            }\n          }\n\n          if (!isManual && line.orderItemId) {\n            await tx.purchaseOrderItem.update({\n              where: { id: line.orderItemId },\n              data: { receivedQty: { decrement: Number(line.quantity) } },\n            });\n            const updatedOI = await tx.purchaseOrderItem.findUnique({\n              where: { id: line.orderItemId },\n            });\n            if (updatedOI)\n              await refreshLineStatus(\n                tx,\n                'purchaseOrderItem',\n                line.orderItemId,\n                Number(updatedOI.openQty),\n              );\n          }\n        }\n        for (const [wId, ids] of whItemMap) {\n          for (const itemId of ids)\n            await recalcOrdered(tx, tenantId, itemId, wId);\n        }\n      }\n\n      // ── Marcar recepción como CANCELADA ──────────────────────────\n      await tx.purchaseReceipt.update({\n        where: {"
      },
      {
        "type": "update",
        "model": "purchaseReceiptItem",
        "snippet": "al) * subtotal;\n              }\n            }\n\n            const totalCost = cost != null ? cost * newQty : null;\n            const totalWeight = weight != null ? weight * newQty : null;\n\n            await tx.purchaseReceiptItem.update({\n              where: { id: line.id },\n              data: {\n                quantity: newQty,\n                warehouseId: effectiveWh ?? null,\n                customFields: line.customFields ?? existing.customFields ?? {},\n                subtotal,\n                taxIndicatorId: taxIndicatorId ?? null,\n                taxRate,\n                taxAmount,\n                totalCost,\n                totalWeight,\n                openQty: newQty,\n                batchId:\n                  line.batchId !== undefined ? line.batchId : existing.batchId,\n                serialNumberId:\n                  line.serialNumberId !== undefined\n                    ? line.serialNumberId\n                    : existing.serialNumberId,\n              },\n            });\n\n            headerSubtotal += subtotal;"
      },
      {
        "type": "create",
        "model": "purchaseReceiptItem",
        "snippet": "                 : 0;\n              }\n            }\n\n            const totalCost = cost != null ? cost * qty : null;\n            const totalWeight = weight != null ? weight * qty : null;\n\n            await tx.purchaseReceiptItem.create({\n              data: {\n                tenantId: effectiveTenantId,\n                purchaseReceiptId: receiptId,\n                itemId: item.id,\n                itemName: item.name,\n                itemCode: item.code,\n                quantity: qty,\n                price,\n                priceNet: line.priceNet != null ? Number(line.priceNet) : price,\n                discountPct: line.discountPct ?? null,\n                discountAmt: line.discountAmt ?? null,\n                discountTotal: line.discountTotal ?? 0,\n                subtotal,\n                taxIndicatorId,\n                taxRate,\n                taxAmount,\n                cost,\n                totalCost,\n                weight,\n                totalWeight,\n                warehouseId: effectiveWh,\n                lineNum"
      },
      {
        "type": "create",
        "model": "purchaseReceiptItem",
        "snippet": "          : oi.item.cost != null\n                ? Number(oi.item.cost)\n                : null;\n        const weight = oi.item.weight != null ? Number(oi.item.weight) : null;\n\n        const created = await tx.purchaseReceiptItem.create({\n          data: {\n            tenantId: tenantId as number,\n            purchaseReceiptId: receipt.id,\n            orderItemId: line.orderItemId,\n            itemId: oi.itemId,\n            itemName: oi.item.name,\n            itemCode: oi.item.code,\n            quantity: line.quantity,\n            price: unitCost,\n            cost: unitCost,\n            totalCost: unitCost != null ? unitCost * line.quantity : null,\n            weight,\n            totalWeight: weight != null ? weight * line.quantity : null,\n            warehouseId: lineWh,\n            lineNum: index,\n            openQty: 0,\n            taxIndicatorId: oi.taxIndicatorId ?? null,\n            taxRate: oi.taxRate != null ? Number(oi.taxRate) : null,\n            batchId: line.batchId ?? null,\n            serialNumberId: line.se"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": " await setTargetOnSourceLine(\n          tx,\n          'purchaseOrderItem',\n          ci.orderItemId,\n          DocumentType.PURCHASE_RECEIPT,\n          receipt.id,\n          ci.id,\n        );\n        await tx.purchaseOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: {\n            receivedQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n        const updatedOI = await tx.purchaseOrderItem.findUnique({\n          where: { id: ci.orderItemId },\n        });\n        if (updatedOI)\n          await refreshLineStatus(\n            tx,\n            'purchaseOrderItem',\n            ci.orderItemId,\n            Number(updatedOI.openQty),\n          );\n        await refreshLineStatus(tx, 'purchaseReceiptItem', ci.id, 0);\n\n        if (ci.lineWh) {\n          await tx.stockMovement.create({\n            data: {\n              tenantId: tenantId as number,\n              itemId: ci.itemId,\n              warehouseId: ci.lineWh,\n              type: StockMovementType.PURCH"
      },
      {
        "type": "create",
        "model": "purchaseReceiptItem",
        "snippet": "\n                : null;\n        const weight = ii.item.weight != null ? Number(ii.item.weight) : null;\n\n        const rcptOrderItemId: number | null = ii.orderItemId ?? null;\n        const created = await tx.purchaseReceiptItem.create({\n          data: {\n            tenantId: tenantId as number,\n            purchaseReceiptId: receipt.id,\n            orderItemId: rcptOrderItemId,\n            // purchaseInvoiceItemId no existe en PurchaseReceiptItem schema;\n            itemId: ii.itemId,\n            itemName: ii.item.name,\n            itemCode: ii.item.code,\n            quantity: line.quantity,\n            price: unitCost,\n            cost: unitCost,\n            totalCost: unitCost != null ? unitCost * line.quantity : null,\n            weight,\n            totalWeight: weight != null ? weight * line.quantity : null,\n            warehouseId: lineWh,\n            lineNum: index,\n            openQty: 0,\n            taxIndicatorId: ii.taxIndicatorId ?? null,\n            taxRate: ii.taxRate != null ? Number(ii.taxRate) : null,\n "
      },
      {
        "type": "update",
        "model": "purchaseInvoiceItem",
        "snippet": "argetOnSourceLine(\n          tx,\n          'purchaseInvoiceItem',\n          ci.reserveInvoiceItemId,\n          DocumentType.PURCHASE_RECEIPT,\n          receipt.id,\n          ci.id,\n        );\n        await tx.purchaseInvoiceItem.update({\n          where: { id: ci.reserveInvoiceItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n        const updatedII = await tx.purchaseInvoiceItem.findUnique({\n          where: { id: ci.reserveInvoiceItemId },\n        });\n        if (updatedII)\n          await refreshLineStatus(\n            tx,\n            'purchaseInvoiceItem',\n            ci.reserveInvoiceItemId,\n            Number(updatedII.openQty),\n          );\n        await refreshLineStatus(tx, 'purchaseReceiptItem', ci.id, 0);\n\n        if (ci.lineWh) {\n          await tx.stockMovement.create({\n            data: {\n              tenantId: tenantId as number,\n              itemId: ci.itemId,\n              warehouseId: ci.lineWh,\n              type: StockMovementType.PURCHASE_RECEIPT,\n              quantity: ci."
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "i = invoices\n          .flatMap((inv) => inv.items)\n          .find((i) => i.id === ci.reserveInvoiceItemId)!;\n        const orderItemId = ii.orderItemId ?? null;\n        if (orderItemId) {\n          await tx.purchaseOrderItem.update({\n            where: { id: orderItemId },\n            data: {\n              receivedQty: { increment: ci.qty },\n              openQty: { decrement: ci.qty },\n            },\n          });\n        }\n      }\n\n      if (frcOrderId) {\n        const orderItemsUpdated = await tx.purchaseOrderItem.findMany({\n          where: { orderId: frcOrderId },\n        });\n        const fullyReceived = orderItemsUpdated.every(\n          (oi) => Number(oi.openQty) <= 0,\n        );\n        if (fullyReceived)\n          await tx.purchaseOrder.update({\n            where: { id: frcOrderId },\n            data: { status: 'CLOSED' },\n          });\n        await recalcPurchaseOrderProgress(tx, frcOrderId);\n      }\n\n      for (const [wId, ids] of whItemMap)\n        for (const itemId of ids)\n          await recalcOrdered"
      },
      {
        "type": "create",
        "model": "purchaseReceiptItem",
        "snippet": "          : qi.item.cost != null\n                ? Number(qi.item.cost)\n                : null;\n        const weight = qi.item.weight != null ? Number(qi.item.weight) : null;\n\n        const created = await tx.purchaseReceiptItem.create({\n          data: {\n            tenantId: tenantId as number,\n            purchaseReceiptId: receipt.id,\n            itemId: qi.itemId,\n            itemName: qi.item.name,\n            itemCode: qi.item.code,\n            quantity: line.quantity,\n            price: unitCost,\n            cost: unitCost,\n            totalCost: unitCost != null ? unitCost * line.quantity : null,\n            weight,\n            totalWeight: weight != null ? weight * line.quantity : null,\n            warehouseId: lineWh,\n            lineNum: index,\n            openQty: 0,\n            taxIndicatorId: qi.taxIndicatorId ?? null,\n            taxRate: qi.taxRate != null ? Number(qi.taxRate) : null,\n            batchId: line.batchId ?? null,\n            serialNumberId: line.serialNumberId ?? null,\n            ...buildB"
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "etTargetOnSourceLine(\n          tx,\n          'purchaseQuotationItem',\n          ci.quotationItemId,\n          DocumentType.PURCHASE_RECEIPT,\n          receipt.id,\n          ci.id,\n        );\n        await tx.purchaseQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n        const updatedQI = await tx.purchaseQuotationItem.findUnique({\n          where: { id: ci.quotationItemId },\n        });\n        if (updatedQI)\n          await refreshLineStatus(\n            tx,\n            'purchaseQuotationItem',\n            ci.quotationItemId,\n            Number(updatedQI.openQty),\n          );\n        await refreshLineStatus(tx, 'purchaseReceiptItem', ci.id, 0);\n\n        if (ci.lineWh) {\n          await tx.stockMovement.create({\n            data: {\n              tenantId: tenantId as number,\n              itemId: ci.itemId,\n              warehouseId: ci.lineWh,\n              type: Stoc"
      }
    ]
  },
  {
    "file": "purchase-invoices/purchase-invoices.service.ts",
    "matches": [
      {
        "type": "create",
        "model": "purchaseInvoiceItem",
        "snippet": "geRate ?? quotation.exchangeRate ?? 1,\n          createdById: createdById ?? null,\n          customFields: payload.customFields ?? {},\n        },\n      });\n\n      for (const ld of lineData) {\n        await tx.purchaseInvoiceItem.create({\n          data: { purchaseInvoiceId: invoice.id, ...ld },\n        });\n      }\n\n      // Trazabilidad: link cabecera + consumo de openQty en cotización\n      await linkDocuments(\n        tx,\n        tenantId,\n        DocumentType.PURCHASE_QUOTATION,\n        quotationId,\n        docType,\n        invoice.id,\n      );\n\n      const createdItems = await tx.purchaseInvoiceItem.findMany({\n        where: { purchaseInvoiceId: invoice.id },\n      });\n      for (const ci of createdItems) {\n        if (ci.baseLineId == null) continue;\n        await setTargetOnSourceLine(\n          tx,\n          'purchaseQuotationItem',\n          ci.baseLineId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.purchaseQuotationItem.update({\n          where: { id: ci.baseLineId },\n  "
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": ") continue;\n        await setTargetOnSourceLine(\n          tx,\n          'purchaseQuotationItem',\n          ci.baseLineId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.purchaseQuotationItem.update({\n          where: { id: ci.baseLineId },\n          data: {\n            orderedQty: { increment: ci.quantity },\n            openQty: { decrement: ci.quantity },\n          },\n        });\n        const updatedQI = await tx.purchaseQuotationItem.findUnique({\n          where: { id: ci.baseLineId },\n        });\n        if (updatedQI)\n          await refreshLineStatus(\n            tx,\n            'purchaseQuotationItem',\n            ci.baseLineId,\n            Number(updatedQI.openQty),\n          );\n      }\n\n      await refreshHeaderStatus(\n        tx,\n        'purchaseQuotation',\n        'purchaseQuotationItem',\n        quotationId,\n        'OPEN',\n        'CLOSED',\n      );\n\n      return invoice.id;\n    });\n    if (isIns === IS_CLIENT || isIns === IS_RESERVE)\n      return this._autoConfirmReser"
      },
      {
        "type": "create",
        "model": "purchaseInvoiceItem",
        "snippet": "\n      });\n\n      const createdItems: { id: number; orderItemId: number; qty: number }[] =\n        [];\n\n      for (const { index, line, oi, lc, cost, weight } of lineInputs) {\n        const created = await tx.purchaseInvoiceItem.create({\n          data: {\n            tenantId,\n            purchaseInvoiceId: invoice.id,\n            orderItemId: line.orderItemId,\n            itemId: oi.itemId,\n            itemName: oi.itemName,\n            itemCode: oi.itemCode,\n            quantity: line.quantity,\n            price: Number(oi.price),\n            subtotal: lc.lineNet,\n            priceNet: lc.priceNet,\n            discountPct:\n              line.discountPct !== undefined\n                ? line.discountPct\n                : oi.discountPct != null\n                  ? Number(oi.discountPct)\n                  : null,\n            discountAmt:\n              line.discountAmt !== undefined\n                ? line.discountAmt\n                : oi.discountAmt != null\n                  ? Number(oi.discountAmt)\n                  : null"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "m',\n          ci.orderItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n\n        // Decrementar openQty en la línea de la orden (ahora reservado en la factura OPEN)\n        await tx.purchaseOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n        const updatedOI = await tx.purchaseOrderItem.findUnique({\n          where: { id: ci.orderItemId },\n        });\n        await refreshLineStatus(\n          tx,\n          'purchaseOrderItem',\n          ci.orderItemId,\n          Number(updatedOI?.openQty ?? 0),\n        );\n      }\n\n      return invoice.id;\n    });\n    if (isIns === IS_CLIENT || isIns === IS_RESERVE)\n      return this._autoConfirmReserve(createdInvoiceId, tenantId, createdById);\n    return this.findOne(createdInvoiceId);\n  }\n\n  // ─── AGREGAR en purchase-invoices.service.ts ─────────────────────────────────\n  // Insertar después de createFromOrder()\n\n  // ======================================================\n  // C"
      },
      {
        "type": "create",
        "model": "purchaseInvoiceItem",
        "snippet": "angeRate ?? receipt.exchangeRate ?? 1,\n          createdById: createdById ?? null,\n          customFields: payload.customFields ?? {},\n        },\n      });\n\n      for (const ld of lineData) {\n        await tx.purchaseInvoiceItem.create({\n          data: { purchaseInvoiceId: invoice.id, ...ld },\n        });\n      }\n\n      // Actualizar purchaseReceiptItem (openQty, invoicedQty) y propagar a purchaseOrderItem\n      for (const ld of lineData) {\n        if (!ld.purchaseReceiptItemId) continue;\n        const ri = await tx.purchaseReceiptItem.findUnique({\n          where: { id: ld.purchaseReceiptItemId },\n        });\n        if (!ri) continue;\n        const newOpenQty = Math.max(0, Number(ri.openQty ?? 0) - ld.quantity);\n        const newInvoicedQty = Number(ri.invoicedQty ?? 0) + ld.quantity;\n        await tx.purchaseReceiptItem.update({\n          where: { id: ld.purchaseReceiptItemId },\n          data: {\n            openQty: newOpenQty,\n            invoicedQty: newInvoicedQty,\n          },\n        });\n        await refreshLi"
      },
      {
        "type": "update",
        "model": "purchaseReceiptItem",
        "snippet": "        });\n        if (!ri) continue;\n        const newOpenQty = Math.max(0, Number(ri.openQty ?? 0) - ld.quantity);\n        const newInvoicedQty = Number(ri.invoicedQty ?? 0) + ld.quantity;\n        await tx.purchaseReceiptItem.update({\n          where: { id: ld.purchaseReceiptItemId },\n          data: {\n            openQty: newOpenQty,\n            invoicedQty: newInvoicedQty,\n          },\n        });\n        await refreshLineStatus(\n          tx,\n          'purchaseReceiptItem',\n          ld.purchaseReceiptItemId,\n          newOpenQty,\n        );\n        const poItemId = ri.orderItemId;\n        if (poItemId) {\n          const oi = await tx.purchaseOrderItem.findUnique({\n            where: { id: poItemId },\n          });\n          if (oi) {\n            const newOiInvoiced = Number(oi.invoicedQty ?? 0) + ld.quantity;\n            await tx.purchaseOrderItem.update({\n              where: { id: poItemId },\n              data: { invoicedQty: newOiInvoiced },\n            });\n            await refreshLineStatus(\n              t"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "await tx.purchaseOrderItem.findUnique({\n            where: { id: poItemId },\n          });\n          if (oi) {\n            const newOiInvoiced = Number(oi.invoicedQty ?? 0) + ld.quantity;\n            await tx.purchaseOrderItem.update({\n              where: { id: poItemId },\n              data: { invoicedQty: newOiInvoiced },\n            });\n            await refreshLineStatus(\n              tx,\n              'purchaseOrderItem',\n              poItemId,\n              Number(oi.openQty),\n            );\n          }\n        }\n      }\n\n      // Trazabilidad: link con recepción y opcionalmente con la orden\n      await linkDocuments(\n        tx,\n        tenantId,\n        DocumentType.PURCHASE_RECEIPT,\n        receiptId,\n        docType,\n        invoice.id,\n      );\n      if (receipt.purchaseOrderId) {\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.PURCHASE_ORDER,\n          receipt.purchaseOrderId,\n          docType,\n          invoice.id,\n        );\n      }\n\n      return invoice.id;\n    }"
      },
      {
        "type": "create",
        "model": "purchaseInvoiceItem",
        "snippet": "exchangeRate ?? sri.exchangeRate ?? 1,\n          createdById: createdById ?? null,\n          customFields: payload.customFields ?? {},\n        },\n      });\n\n      for (const ld of lineData) {\n        await tx.purchaseInvoiceItem.create({\n          data: { purchaseInvoiceId: invoice.id, ...ld },\n        });\n      }\n\n      await linkDocuments(\n        tx,\n        tenantId,\n        DocumentType.PURCHASE_RESERVE_INVOICE,\n        reserveInvoiceId,\n        DocumentType.PURCHASE_INVOICE,\n        invoice.id,\n      );\n      if (sri.orderId) {\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.PURCHASE_ORDER,\n          sri.orderId,\n          DocumentType.PURCHASE_INVOICE,\n          invoice.id,\n        );\n      }\n\n      return invoice.id;\n    });\n    return this.confirm(createdInvoiceId, tenantId, createdById);\n  }\n\n  async createManual(\n    payload: {\n      tenantId: number;\n      isIns?: string;\n      supplierId: number;\n      warehouseId?: number | null;\n      currency?: string;\n      exchangeR"
      },
      {
        "type": "create",
        "model": "purchaseInvoiceItem",
        "snippet": "geRate: payload.exchangeRate ?? 1,\n          createdById: createdById ?? null,\n          customFields: payload.customFields ?? {},\n        },\n      });\n\n      for (const ii of invoiceItems) {\n        await tx.purchaseInvoiceItem.create({\n          data: { purchaseInvoiceId: invoice.id, ...ii },\n        });\n      }\n\n      // Trazabilidad: vincular con cotización origen si viene de \"Copiar a\"\n      if (payload.purchaseQuotationId) {\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.PURCHASE_QUOTATION,\n          payload.purchaseQuotationId,\n          docType,\n          invoice.id,\n        );\n      }\n      if (payload.purchaseReceiptId) {\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.PURCHASE_RECEIPT,\n          payload.purchaseReceiptId,\n          docType,\n          invoice.id,\n        );\n      }\n\n      // Trazabilidad de línea + consumo de cotización\n      const createdItems = await tx.purchaseInvoiceItem.findMany({\n        where: { purchaseInvoiceI"
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "      ci.id,\n            );\n          }\n          // ── Consumir openQty en la cotización origen ──────────────────────\n          if (ci.baseDocType === DocumentType.PURCHASE_QUOTATION) {\n            await tx.purchaseQuotationItem.update({\n              where: { id: ci.baseLineId },\n              data: {\n                orderedQty: { increment: ci.quantity },\n                openQty: { decrement: ci.quantity },\n              },\n            });\n            const updatedQI = await tx.purchaseQuotationItem.findUnique({\n              where: { id: ci.baseLineId },\n            });\n            if (updatedQI)\n              await refreshLineStatus(\n                tx,\n                'purchaseQuotationItem',\n                ci.baseLineId,\n                Number(updatedQI.openQty),\n              );\n          }\n        }\n      }\n\n      return invoice.id;\n    });\n    if (isIns === IS_CLIENT)\n      return this.confirm(createdInvoiceId, payload.tenantId, createdById);\n    return this.findOne(createdInvoiceId);\n  }\n\n  // ================"
      },
      {
        "type": "update",
        "model": "purchaseInvoiceItem",
        "snippet": "invoice.currency ?? 'BOB',\n      { totalInvoicedAP: Number(invoice.total) },\n    );\n\n    // ── Actualizar líneas y trazabilidad ─────────────────────────────────\n    for (const line of items) {\n      await tx.purchaseInvoiceItem.update({\n        where: { id: line.id },\n        data: { openQty: 0 },\n      });\n      await refreshLineStatus(tx, 'purchaseInvoiceItem', line.id, 0);\n\n      if (line.orderItemId) {\n        // Factura directa (sin recepción previa): también incrementar receivedQty\n        const isDirectPurchase =\n          !line.purchaseReceiptItemId && invoice.isIns === 'N';\n        if (isDirectPurchase) {\n          await tx.purchaseOrderItem.update({\n            where: { id: line.orderItemId },\n            data: {\n              invoicedQty: { increment: Number(line.quantity) },\n              receivedQty: { increment: Number(line.quantity) },\n            },\n          });\n          const updatedOI = await tx.purchaseOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (up"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "ecta (sin recepción previa): también incrementar receivedQty\n        const isDirectPurchase =\n          !line.purchaseReceiptItemId && invoice.isIns === 'N';\n        if (isDirectPurchase) {\n          await tx.purchaseOrderItem.update({\n            where: { id: line.orderItemId },\n            data: {\n              invoicedQty: { increment: Number(line.quantity) },\n              receivedQty: { increment: Number(line.quantity) },\n            },\n          });\n          const updatedOI = await tx.purchaseOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (updatedOI)\n            await refreshLineStatus(\n              tx,\n              'purchaseOrderItem',\n              line.orderItemId,\n              Number(updatedOI.openQty),\n            );\n        }\n      }\n    }\n\n    // ── Sincronizar estado de PurchaseOrder según líneas ────────────────\n    if (invoice.orderId) {\n      await refreshHeaderStatus(\n        tx,\n        'purchaseOrder',\n        'purchaseOrderItem',\n        invoice.o"
      },
      {
        "type": "update",
        "model": "purchaseInvoiceItem",
        "snippet": "  'AVAILABLE',\n                  null,\n                );\n              }\n            }\n          }\n        }\n\n        // Restaurar líneas de orden si aplica\n        if (line.orderItemId) {\n          await tx.purchaseInvoiceItem.update({\n            where: { id: line.id },\n            data: { openQty: Number(line.quantity) },\n          });\n          await tx.purchaseOrderItem.update({\n            where: { id: line.orderItemId },\n            data: {\n              openQty: { increment: Number(line.quantity) },\n              invoicedQty: { decrement: Number(line.quantity) },\n            },\n          });\n          await clearTargetOnSourceLine(\n            tx,\n            'purchaseOrderItem',\n            line.orderItemId,\n          );\n          const oi = await tx.purchaseOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (oi)\n            await refreshLineStatus(\n              tx,\n              'purchaseOrderItem',\n              line.orderItemId,\n              Number(oi.openQty),\n "
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "plica\n        if (line.orderItemId) {\n          await tx.purchaseInvoiceItem.update({\n            where: { id: line.id },\n            data: { openQty: Number(line.quantity) },\n          });\n          await tx.purchaseOrderItem.update({\n            where: { id: line.orderItemId },\n            data: {\n              openQty: { increment: Number(line.quantity) },\n              invoicedQty: { decrement: Number(line.quantity) },\n            },\n          });\n          await clearTargetOnSourceLine(\n            tx,\n            'purchaseOrderItem',\n            line.orderItemId,\n          );\n          const oi = await tx.purchaseOrderItem.findUnique({\n            where: { id: line.orderItemId },\n          });\n          if (oi)\n            await refreshLineStatus(\n              tx,\n              'purchaseOrderItem',\n              line.orderItemId,\n              Number(oi.openQty),\n            );\n        }\n\n        // Restaurar líneas de recepción si aplica\n        if (line.purchaseReceiptItemId) {\n          await tx.purchaseRecei"
      },
      {
        "type": "update",
        "model": "purchaseReceiptItem",
        "snippet": "em',\n              line.orderItemId,\n              Number(oi.openQty),\n            );\n        }\n\n        // Restaurar líneas de recepción si aplica\n        if (line.purchaseReceiptItemId) {\n          await tx.purchaseReceiptItem.update({\n            where: { id: line.purchaseReceiptItemId },\n            data: {\n              openQty: { increment: Number(line.quantity) },\n              invoicedQty: { decrement: Number(line.quantity) },\n            },\n          });\n          await clearTargetOnSourceLine(\n            tx,\n            'purchaseReceiptItem',\n            line.purchaseReceiptItemId,\n          );\n          const ri = await tx.purchaseReceiptItem.findUnique({\n            where: { id: line.purchaseReceiptItemId },\n          });\n          if (ri) {\n            await refreshLineStatus(\n              tx,\n              'purchaseReceiptItem',\n              line.purchaseReceiptItemId,\n              Number(ri.openQty),\n            );\n            // Revertir invoicedQty en PurchaseOrderItem si la recepción tiene línea de "
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "y),\n            );\n            // Revertir invoicedQty en PurchaseOrderItem si la recepción tiene línea de orden\n            const poItemId = ri.orderItemId;\n            if (poItemId) {\n              await tx.purchaseOrderItem.update({\n                where: { id: poItemId },\n                data: { invoicedQty: { decrement: Number(line.quantity) } },\n              });\n              const updatedOI = await tx.purchaseOrderItem.findUnique({\n                where: { id: poItemId },\n              });\n              if (updatedOI)\n                await refreshLineStatus(\n                  tx,\n                  'purchaseOrderItem',\n                  poItemId,\n                  Number(updatedOI.openQty),\n                );\n            }\n          }\n        }\n      }\n\n      // Sincronizar estado de PurchaseOrder según líneas\n      if (invoice.orderId) {\n        await refreshHeaderStatus(\n          tx,\n          'purchaseOrder',\n          'purchaseOrderItem',\n          invoice.orderId,\n          'OPEN',\n          'CLOSED',\n    "
      },
      {
        "type": "update",
        "model": "purchaseInvoiceItem",
        "snippet": "ate,\n            isInclusive: effectiveIsInclusive,\n          });\n          const priceNet = lc.priceNet;\n          const subtotal = lc.lineFinal;\n          const taxAmount = lc.taxAmount;\n\n          await tx.purchaseInvoiceItem.update({\n            where: { id: line.id },\n            data: {\n              quantity: qty,\n              discountPct,\n              discountAmt,\n              discountTotal,\n              priceNet,\n              subtotal,\n              ...(taxAmount != null ? { taxAmount } : {}),\n              taxIndicatorId: effectiveTaxIndicatorId,\n              taxRate: effectiveTaxRate,\n              ...(line.warehouseId !== undefined\n                ? { warehouseId: line.warehouseId }\n                : {}),\n              ...(line.customFields !== undefined\n                ? { customFields: line.customFields }\n                : {}),\n              ...(line.projectCode !== undefined\n                ? { projectCode: line.projectCode }\n                : {}),\n              ...(line.dimension1 !== undefined\n    "
      },
      {
        "type": "create",
        "model": "purchaseInvoiceItem",
        "snippet": "   totalCost: computeConditionalTotal(lineData, 'totalCost'),\n          totalWeight: computeConditionalTotal(lineData, 'totalWeight'),\n        },\n      });\n\n      for (const ld of lineData) {\n        await tx.purchaseInvoiceItem.create({\n          data: { purchaseInvoiceId: invoice.id, ...ld },\n        });\n      }\n\n      // Trazabilidad por cada recepción origen\n      for (const receiptId of receiptIds) {\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.PURCHASE_RECEIPT,\n          receiptId,\n          docType,\n          invoice.id,\n        );\n      }\n\n      // Linkar también a la orden de compra origen si las recepciones pertenecen a una orden\n      const purchaseOrderIds = [\n        ...new Set(receipts.map((r) => r.purchaseOrderId).filter(Boolean)),\n      ] as number[];\n      for (const orderId of purchaseOrderIds)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.PURCHASE_ORDER,\n          orderId,\n          docType,\n          invoice.id,\n        )"
      },
      {
        "type": "update",
        "model": "purchaseReceiptItem",
        "snippet": " setTargetOnSourceLine(\n            tx,\n            'purchaseReceiptItem',\n            il.purchaseReceiptItemId,\n            docType,\n            invoice.id,\n            il.id,\n          );\n          await tx.purchaseReceiptItem.update({\n            where: { id: il.purchaseReceiptItemId },\n            data: { invoicedQty: { increment: Number(il.quantity) } },\n          });\n          await refreshLineStatus(\n            tx,\n            'purchaseInvoiceItem',\n            il.id,\n            Number(il.openQty),\n          );\n        }\n      }\n\n      // Recalcular progreso de facturación en las órdenes origen\n      for (const orderId of purchaseOrderIds)\n        await recalcPurchaseOrderProgress(tx, orderId);\n\n      return invoice.id;\n    });\n\n    if (isIns === IS_RESERVE)\n      return this._autoConfirmReserve(createdInvoiceId, tenantId, createdById);\n    return this.findOne(createdInvoiceId);\n  }\n  // ======================================================\n  // DRAFT — N Cotizaciones → 1 FRC\n  // =============================="
      },
      {
        "type": "create",
        "model": "purchaseInvoiceItem",
        "snippet": "\n        id: number;\n        quotationItemId: number;\n        qty: number;\n      }[] = [];\n      for (const ld of lineData) {\n        const { _quotationItemId, ...data } = ld;\n        const created = await tx.purchaseInvoiceItem.create({\n          data: { purchaseInvoiceId: invoice.id, ...data },\n        });\n        createdItems.push({\n          id: created.id,\n          quotationItemId: _quotationItemId!,\n          qty: Number(ld.quantity),\n        });\n      }\n\n      for (const quotationId of quotationIds)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.PURCHASE_QUOTATION,\n          quotationId,\n          docType,\n          invoice.id,\n        );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'purchaseQuotationItem',\n          ci.quotationItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.purchaseQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n      "
      },
      {
        "type": "update",
        "model": "purchaseQuotationItem",
        "snippet": "ems) {\n        await setTargetOnSourceLine(\n          tx,\n          'purchaseQuotationItem',\n          ci.quotationItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.purchaseQuotationItem.update({\n          where: { id: ci.quotationItemId },\n          data: {\n            orderedQty: { increment: ci.qty },\n            openQty: { decrement: ci.qty },\n          },\n        });\n        const updatedQI = await tx.purchaseQuotationItem.findUnique({\n          where: { id: ci.quotationItemId },\n        });\n        if (updatedQI)\n          await refreshLineStatus(\n            tx,\n            'purchaseQuotationItem',\n            ci.quotationItemId,\n            Number(updatedQI.openQty),\n          );\n      }\n\n      for (const quotationId of quotationIds) {\n        const allQI = await tx.purchaseQuotationItem.findMany({\n          where: { quotationId },\n        });\n        if (allQI.every((i) => Number(i.openQty) <= 0))\n          await tx.purchaseQuotation.update({\n            where: { id: quo"
      },
      {
        "type": "create",
        "model": "purchaseInvoiceItem",
        "snippet": ");\n\n      const createdItems: { id: number; orderItemId: number; qty: number }[] =\n        [];\n      for (const ld of lineData) {\n        const { _orderItemId, ...data } = ld;\n        const created = await tx.purchaseInvoiceItem.create({\n          data: { purchaseInvoiceId: invoice.id, ...data },\n        });\n        createdItems.push({\n          id: created.id,\n          orderItemId: _orderItemId!,\n          qty: Number(ld.quantity),\n        });\n      }\n\n      for (const orderId of orderIds)\n        await linkDocuments(\n          tx,\n          tenantId,\n          DocumentType.PURCHASE_ORDER,\n          orderId,\n          docType,\n          invoice.id,\n        );\n\n      for (const ci of createdItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'purchaseOrderItem',\n          ci.orderItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.purchaseOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n"
      },
      {
        "type": "update",
        "model": "purchaseOrderItem",
        "snippet": "reatedItems) {\n        await setTargetOnSourceLine(\n          tx,\n          'purchaseOrderItem',\n          ci.orderItemId,\n          docType,\n          invoice.id,\n          ci.id,\n        );\n        await tx.purchaseOrderItem.update({\n          where: { id: ci.orderItemId },\n          data: { openQty: { decrement: ci.qty } },\n        });\n        const updatedOI = await tx.purchaseOrderItem.findUnique({\n          where: { id: ci.orderItemId },\n        });\n        if (updatedOI)\n          await refreshLineStatus(\n            tx,\n            'purchaseOrderItem',\n            ci.orderItemId,\n            Number(updatedOI.openQty),\n          );\n      }\n\n      return invoice.id;\n    });\n\n    if (isIns === IS_CLIENT || isIns === IS_RESERVE)\n      return this._autoConfirmReserve(createdInvoiceId, tenantId, createdById);\n    return this.findOne(createdInvoiceId);\n  }\n\n  // ======================================================\n  // Recalcular totales del header desde las líneas\n  // =============================================="
      }
    ]
  },
  {
    "file": "sales-returns/sales-returns.service.ts",
    "matches": [
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "ty);\n          const lineInvoiced = Number(dLine.invoicedQty);\n          const maxAdd = lineQty - lineInvoiced;\n          const add = Math.min(remaining, maxAdd);\n          if (add > 0) {\n            await tx.deliveryOrderItem.update({\n              where: { id: dLine.id },\n              data: { invoicedQty: lineInvoiced + add },\n            });\n            const pending = lineQty - (lineInvoiced + add);\n            await refreshLineStatus(tx, 'deliveryOrderItem', dLine.id, pending);\n            remaining -= add;\n          }\n        }\n      }\n\n      await tx.documentLink.create({\n        data: {\n          tenantId,\n          sourceType: 'DELIVERY_ORDER',\n          sourceId: ret.deliveryOrderId,\n          targetType: 'SALES_RETURN',\n          targetId: returnId,\n        },\n      });\n    }\n  }\n\n  // ── Cancelar ─────────────────────────────────────────────────\n  async cancel(returnId: number, updatedById?: number, tenantId?: number) {\n    const where: Prisma.SalesReturnWhereInput = { id: returnId };\n    if (tenantId) whe"
      },
      {
        "type": "update",
        "model": "deliveryOrderItem",
        "snippet": "   if (remaining <= 0) break;\n            const lineInvoiced = Number(dLine.invoicedQty);\n            const subtract = Math.min(remaining, lineInvoiced);\n            if (subtract > 0) {\n              await tx.deliveryOrderItem.update({\n                where: { id: dLine.id },\n                data: { invoicedQty: lineInvoiced - subtract },\n              });\n              const pending =\n                Number(dLine.quantity) - (lineInvoiced - subtract);\n              await refreshLineStatus(\n                tx,\n                'deliveryOrderItem',\n                dLine.id,\n                pending,\n              );\n              remaining -= subtract;\n            }\n          }\n        }\n      }\n\n      return this.findOneInternal(tx, returnId, docTenantId);\n    });\n  }\n\n  // ── Actualizar ───────────────────────────────────────────────\n  async update(\n    returnId: number,\n    dto: UpdateSalesReturnDto,\n    updatedById?: number,\n    tenantId?: number,\n  ) {\n    const where: Prisma.SalesReturnWhereInput = { id: returnId };"
      }
    ]
  },
  {
    "file": "sales-credit-notes/sales-credit-notes.service.ts",
    "matches": []
  },
  {
    "file": "purchase-returns/purchase-returns.service.ts",
    "matches": [
      {
        "type": "update",
        "model": "purchaseReceiptItem",
        "snippet": "ty);\n          const lineInvoiced = Number(rLine.invoicedQty);\n          const maxAdd = lineQty - lineInvoiced;\n          const add = Math.min(remaining, maxAdd);\n          if (add > 0) {\n            await tx.purchaseReceiptItem.update({\n              where: { id: rLine.id },\n              data: { invoicedQty: lineInvoiced + add },\n            });\n            const pending = lineQty - (lineInvoiced + add);\n            await refreshLineStatus(\n              tx,\n              'purchaseReceiptItem',\n              rLine.id,\n              pending,\n            );\n            remaining -= add;\n          }\n        }\n      }\n\n      await tx.documentLink.create({\n        data: {\n          tenantId,\n          sourceType: 'PURCHASE_RECEIPT',\n          sourceId: ret.purchaseReceiptId,\n          targetType: 'PURCHASE_RETURN',\n          targetId: returnId,\n        },\n      });\n    }\n  }\n\n  // ── Cancelar ─────────────────────────────────────────────────\n  async cancel(returnId: number, updatedById?: number, tenantId?: number) {\n    con"
      },
      {
        "type": "update",
        "model": "purchaseReceiptItem",
        "snippet": "   if (remaining <= 0) break;\n            const lineInvoiced = Number(rLine.invoicedQty);\n            const subtract = Math.min(remaining, lineInvoiced);\n            if (subtract > 0) {\n              await tx.purchaseReceiptItem.update({\n                where: { id: rLine.id },\n                data: { invoicedQty: lineInvoiced - subtract },\n              });\n              const pending =\n                Number(rLine.quantity) - (lineInvoiced - subtract);\n              await refreshLineStatus(\n                tx,\n                'purchaseReceiptItem',\n                rLine.id,\n                pending,\n              );\n              remaining -= subtract;\n            }\n          }\n        }\n      }\n\n      return this.findOneInternal(tx, returnId, docTenantId);\n    });\n  }\n\n  // ── Actualizar ───────────────────────────────────────────────\n  async update(\n    returnId: number,\n    dto: UpdatePurchaseReturnDto,\n    updatedById?: number,\n    tenantId?: number,\n  ) {\n    const where: Prisma.PurchaseReturnWhereInput = { id: ret"
      }
    ]
  },
  {
    "file": "purchase-credit-notes/purchase-credit-notes.service.ts",
    "matches": []
  },
  {
    "file": "stock-transfers/stock-transfers.service.ts",
    "matches": []
  },
  {
    "file": "stock-entries/stock-entries.service.ts",
    "matches": []
  },
  {
    "file": "stock-exits/stock-exits.service.ts",
    "matches": []
  },
  {
    "file": "stock-adjustments/stock-adjustments.service.ts",
    "matches": []
  }
]